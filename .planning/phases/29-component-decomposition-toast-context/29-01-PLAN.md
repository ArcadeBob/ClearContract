---
phase: 29-component-decomposition-toast-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LegalMetaBadge/index.tsx
  - src/components/LegalMetaBadge/shared.ts
  - src/components/LegalMetaBadge/IndemnificationBadge.tsx
  - src/components/LegalMetaBadge/PaymentContingencyBadge.tsx
  - src/components/LegalMetaBadge/LiquidatedDamagesBadge.tsx
  - src/components/LegalMetaBadge/RetainageBadge.tsx
  - src/components/LegalMetaBadge/InsuranceBadge.tsx
  - src/components/LegalMetaBadge/TerminationBadge.tsx
  - src/components/LegalMetaBadge/FlowDownBadge.tsx
  - src/components/LegalMetaBadge/NoDamageDelayBadge.tsx
  - src/components/LegalMetaBadge/LienRightsBadge.tsx
  - src/components/LegalMetaBadge/DisputeResolutionBadge.tsx
  - src/components/LegalMetaBadge/ChangeOrderBadge.tsx
  - src/components/ScopeMetaBadge/index.tsx
  - src/components/ScopeMetaBadge/shared.ts
  - src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx
  - src/components/ScopeMetaBadge/DatesDeadlinesBadge.tsx
  - src/components/ScopeMetaBadge/VerbiageBadge.tsx
  - src/components/ScopeMetaBadge/LaborComplianceBadge.tsx
autonomous: true
requirements:
  - DECOMP-02
  - DECOMP-03

must_haves:
  truths:
    - "LegalMetaBadge renders identically for all 11 clauseType variants after split"
    - "ScopeMetaBadge renders identically for all 4 passType variants after split"
    - "Existing import paths (from './LegalMetaBadge' and from '../components/LegalMetaBadge') resolve unchanged"
    - "Existing import paths (from './ScopeMetaBadge' and from '../components/ScopeMetaBadge') resolve unchanged"
  artifacts:
    - path: "src/components/LegalMetaBadge/index.tsx"
      provides: "Dispatcher using Record<ClauseType, FC> component map"
      exports: ["LegalMetaBadge"]
    - path: "src/components/LegalMetaBadge/shared.ts"
      provides: "Shared pillBase constant and boolPill helper"
      exports: ["pillBase"]
    - path: "src/components/ScopeMetaBadge/index.tsx"
      provides: "Dispatcher using Record<PassType, FC> component map"
      exports: ["ScopeMetaBadge"]
    - path: "src/components/ScopeMetaBadge/shared.ts"
      provides: "Shared pillBase constant and formatLabel helper"
      exports: ["pillBase", "formatLabel"]
  key_links:
    - from: "src/components/FindingCard.tsx"
      to: "src/components/LegalMetaBadge/index.tsx"
      via: "unchanged import path"
      pattern: "from.*LegalMetaBadge"
    - from: "src/components/FindingCard.tsx"
      to: "src/components/ScopeMetaBadge/index.tsx"
      via: "unchanged import path"
      pattern: "from.*ScopeMetaBadge"
---

<objective>
Split LegalMetaBadge.tsx (417 lines, 11 clauseType branches) and ScopeMetaBadge.tsx (199 lines, 4 passType branches) into directory-based modules with barrel-exported dispatchers.

Purpose: Eliminate nested ternary chains and make each clause/scope type independently readable and maintainable.
Output: Two directory modules (LegalMetaBadge/, ScopeMetaBadge/) with dispatcher index.tsx, shared.ts, and focused subcomponents.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/LegalMetaBadge.tsx
@src/components/ScopeMetaBadge.tsx
@src/types/contract.ts

<interfaces>
<!-- Key types the executor needs from contract.ts -->

LegalMeta is a discriminated union on `clauseType` with 11 variants:
'indemnification' | 'payment-contingency' | 'liquidated-damages' | 'retainage' | 'insurance' | 'termination' | 'flow-down' | 'no-damage-delay' | 'lien-rights' | 'dispute-resolution' | 'change-order'

ScopeMeta is a discriminated union on `passType` with 4 variants:
'scope-of-work' | 'dates-deadlines' | 'verbiage' | 'labor-compliance'

ComplianceChecklistItem is imported by the labor-compliance branch of ScopeMetaBadge.

From src/components/FindingCard.tsx (consumer):
```typescript
import { LegalMetaBadge } from './LegalMetaBadge';
import { ScopeMetaBadge } from './ScopeMetaBadge';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split LegalMetaBadge into directory module</name>
  <files>
    src/components/LegalMetaBadge.tsx (DELETE)
    src/components/LegalMetaBadge/index.tsx
    src/components/LegalMetaBadge/shared.ts
    src/components/LegalMetaBadge/IndemnificationBadge.tsx
    src/components/LegalMetaBadge/PaymentContingencyBadge.tsx
    src/components/LegalMetaBadge/LiquidatedDamagesBadge.tsx
    src/components/LegalMetaBadge/RetainageBadge.tsx
    src/components/LegalMetaBadge/InsuranceBadge.tsx
    src/components/LegalMetaBadge/TerminationBadge.tsx
    src/components/LegalMetaBadge/FlowDownBadge.tsx
    src/components/LegalMetaBadge/NoDamageDelayBadge.tsx
    src/components/LegalMetaBadge/LienRightsBadge.tsx
    src/components/LegalMetaBadge/DisputeResolutionBadge.tsx
    src/components/LegalMetaBadge/ChangeOrderBadge.tsx
  </files>
  <action>
    CRITICAL ORDER: Delete `src/components/LegalMetaBadge.tsx` BEFORE creating `src/components/LegalMetaBadge/` directory. Otherwise both the file and directory would match the import path, causing ambiguous resolution.

    1. Read the full LegalMetaBadge.tsx source to capture all branch logic.

    2. Create `src/components/LegalMetaBadge/shared.ts`:
       - Export `pillBase` constant: `'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium mr-2'`

    3. Create 11 subcomponent files, one per clauseType. Each subcomponent:
       - Imports `LegalMeta` from `../../types/contract`
       - Imports `pillBase` from `./shared`
       - Defines a narrowed type using `Extract<LegalMeta, { clauseType: '...' }>`
       - Props interface: `{ meta: NarrowedType }`
       - Returns a fragment (`<>...</>`) with the JSX from the corresponding branch in the original file
       - Does NOT include the outer `<div className="flex flex-wrap items-center gap-1 mt-2">` wrapper -- that stays in the dispatcher

       The 11 files and their source lines from original:
       - IndemnificationBadge.tsx (lines 13-36)
       - PaymentContingencyBadge.tsx (lines 38-59)
       - LiquidatedDamagesBadge.tsx (lines 61-83)
       - RetainageBadge.tsx (lines 85-113)
       - InsuranceBadge.tsx (lines 115-155) -- also imports from `../../types/contract` for any coverage item types
       - TerminationBadge.tsx (lines 157-192)
       - FlowDownBadge.tsx (lines 194-234)
       - NoDamageDelayBadge.tsx (lines 236-275)
       - LienRightsBadge.tsx (lines 277-317)
       - DisputeResolutionBadge.tsx (lines 319-375)
       - ChangeOrderBadge.tsx (lines 377-414)

    4. Create `src/components/LegalMetaBadge/index.tsx`:
       - Import all 11 subcomponents
       - Import `LegalMeta` from `../../types/contract`
       - Define `type ClauseType = LegalMeta['clauseType']`
       - Create `BADGE_MAP: Record<ClauseType, React.FC<{ meta: any }>>` mapping each clauseType string to its component
       - Export `LegalMetaBadge` function that:
         a. Receives `{ meta: LegalMeta }` props
         b. Looks up `Badge = BADGE_MAP[meta.clauseType]`
         c. Returns null if no Badge found
         d. Wraps in `<div className="flex flex-wrap items-center gap-1 mt-2"><Badge meta={meta} /></div>`
       - Export is named `LegalMetaBadge` (same as before)

    5. Delete the original `src/components/LegalMetaBadge.tsx` file.

    IMPORTANT: Do not change any JSX structure, class names, or conditional logic within subcomponents. This is a pure mechanical extraction -- the rendered output must be pixel-identical.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>LegalMetaBadge directory module exists with index.tsx dispatcher, shared.ts, and 11 subcomponent files. Original LegalMetaBadge.tsx is deleted. Build succeeds. FindingCard import resolves unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Split ScopeMetaBadge into directory module</name>
  <files>
    src/components/ScopeMetaBadge.tsx (DELETE)
    src/components/ScopeMetaBadge/index.tsx
    src/components/ScopeMetaBadge/shared.ts
    src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx
    src/components/ScopeMetaBadge/DatesDeadlinesBadge.tsx
    src/components/ScopeMetaBadge/VerbiageBadge.tsx
    src/components/ScopeMetaBadge/LaborComplianceBadge.tsx
  </files>
  <action>
    Same pattern as Task 1 but for ScopeMetaBadge. CRITICAL ORDER: Delete original file before creating directory.

    1. Read the full ScopeMetaBadge.tsx source to capture all branch logic.

    2. Create `src/components/ScopeMetaBadge/shared.ts`:
       - Export `pillBase` constant: `'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium mr-2'`
       - Export `formatLabel` function: `(value: string): string => value.replace(/-/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase())`
       - NOTE: `pillBase` is intentionally duplicated from LegalMetaBadge/shared.ts -- no cross-directory imports per user decision.

    3. Create 4 subcomponent files, each with narrowed `Extract<ScopeMeta, { passType: '...' }>` type:

       - `ScopeOfWorkBadge.tsx` (lines 15-46): Returns the JSX from the `scope-of-work` branch. This branch returns a `<div>` wrapper -- keep it in the subcomponent since it is self-contained. Import `pillBase` and `formatLabel` from `./shared`.
       - `DatesDeadlinesBadge.tsx` (lines 48-81): Returns a fragment `<>...</>` with the dates-deadlines JSX.
       - `VerbiageBadge.tsx` (lines 83-124): Returns a fragment `<>...</>` with the verbiage JSX.
       - `LaborComplianceBadge.tsx` (lines 126-196): Returns a fragment `<>...</>`. Import `ComplianceChecklistItem` from `../../types/contract`.

    4. Create `src/components/ScopeMetaBadge/index.tsx`:
       - Import all 4 subcomponents
       - Import `ScopeMeta` from `../../types/contract`
       - Define `type PassType = ScopeMeta['passType']`
       - Create `BADGE_MAP: Record<PassType, React.FC<{ meta: any }>>` mapping each passType to its component
       - Export `ScopeMetaBadge` function:
         a. Receives `{ meta: ScopeMeta }` props
         b. Looks up `Badge = BADGE_MAP[meta.passType]`
         c. Returns null if no Badge found
         d. Returns `<Badge meta={meta} />` -- NO outer wrapper div here because subcomponents handle their own structure (scope-of-work returns a div, others return fragments)

    5. Delete the original `src/components/ScopeMetaBadge.tsx` file.

    IMPORTANT: Preserve all JSX exactly. The scope-of-work branch returns its own `<div className="flex flex-wrap items-center gap-1 mt-2">` wrapper. The other three branches return fragments. The dispatcher must NOT add another wrapper.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>ScopeMetaBadge directory module exists with index.tsx dispatcher, shared.ts, and 4 subcomponent files. Original ScopeMetaBadge.tsx is deleted. Build and lint pass. FindingCard import resolves unchanged.</done>
</task>

</tasks>

<verification>
- `npm run build` passes (TypeScript compilation + Vite bundle)
- `npm run lint` passes with no new warnings
- `src/components/LegalMetaBadge.tsx` (original file) no longer exists
- `src/components/ScopeMetaBadge.tsx` (original file) no longer exists
- `src/components/LegalMetaBadge/index.tsx` exports `LegalMetaBadge`
- `src/components/ScopeMetaBadge/index.tsx` exports `ScopeMetaBadge`
- No import path changes in FindingCard.tsx or any other consumer
</verification>

<success_criteria>
- Build succeeds with zero errors
- LegalMetaBadge directory has 13 files (index + shared + 11 subcomponents)
- ScopeMetaBadge directory has 6 files (index + shared + 4 subcomponents)
- Original monolithic files are deleted
- All existing import paths resolve unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/29-component-decomposition-toast-context/29-01-SUMMARY.md`
</output>
