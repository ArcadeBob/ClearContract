---
phase: 55-partial-status-type-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/contract.ts
  - src/components/ContractCard.tsx
  - src/App.tsx
  - src/pages/Dashboard.tsx
  - src/utils/dateUrgency.ts
  - src/components/PatternsCard.tsx
autonomous: true
requirements:
  - PERF-03
gap_closure: true

must_haves:
  truths:
    - "Contract.status type includes 'Partial' as a valid union member"
    - "ContractCard shows amber badge for Partial status contracts"
    - "Dashboard stats include Partial contracts alongside Reviewed ones"
    - "Deadline timeline includes dates from Partial contracts"
    - "PatternsCard includes Partial contracts in pattern detection"
    - "Sidebar urgent deadline badge counts dates from Partial contracts"
  artifacts:
    - path: "src/types/contract.ts"
      provides: "Partial in Contract.status union type"
      contains: "'Partial'"
    - path: "src/components/ContractCard.tsx"
      provides: "Amber badge rendering for Partial status"
      contains: "bg-amber-100 text-amber-700"
    - path: "src/App.tsx"
      provides: "Sidebar badge filter includes Partial"
      contains: "c.status === 'Partial'"
    - path: "src/pages/Dashboard.tsx"
      provides: "Dashboard stats filter includes Partial"
      contains: "c.status === 'Partial'"
    - path: "src/utils/dateUrgency.ts"
      provides: "Timeline filter includes Partial"
      contains: "c.status === 'Partial'"
    - path: "src/components/PatternsCard.tsx"
      provides: "Pattern detection filter includes Partial"
      contains: "c.status === 'Partial'"
  key_links:
    - from: "src/types/contract.ts"
      to: "all consuming components"
      via: "Contract.status type union"
      pattern: "'Analyzing' \\| 'Reviewed' \\| 'Partial' \\| 'Draft'"
    - from: "src/components/ContractCard.tsx"
      to: "visual badge rendering"
      via: "ternary chain for status colors"
      pattern: "status === 'Partial'"
---

<objective>
Add 'Partial' to the client-side Contract.status type union and update all UI components to handle it correctly -- amber badge on ContractCard, inclusion in dashboard stats, timeline, sidebar badge, and pattern detection filters.

Purpose: Closes integration gap from v2.2 audit. The server (api/analyze.ts line 615) already writes 'Partial' status on global timeout, but the client-side type system and UI do not model it, causing Partial contracts to be invisible in stats/timeline and show the wrong badge color.

Output: Updated type definition, badge rendering, and 4 filter locations across 6 files.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-partial-status-type-gap-closure/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Partial to Contract.status type and update ContractCard badge</name>
  <files>src/types/contract.ts, src/components/ContractCard.tsx</files>
  <read_first>
    - src/types/contract.ts (line 178-195 -- Contract interface with status field)
    - src/components/ContractCard.tsx (line 70-80 -- badge ternary chain)
  </read_first>
  <action>
1. In `src/types/contract.ts` line 184, change:
   ```
   status: 'Analyzing' | 'Reviewed' | 'Draft';
   ```
   to:
   ```
   status: 'Analyzing' | 'Reviewed' | 'Partial' | 'Draft';
   ```

2. In `src/components/ContractCard.tsx` line 75, update the badge ternary chain from:
   ```
   contract.status === 'Reviewed' ? 'bg-emerald-100 text-emerald-700' : contract.status === 'Analyzing' ? 'bg-blue-100 text-blue-700' : 'bg-slate-100 text-slate-600'
   ```
   to:
   ```
   contract.status === 'Reviewed'
     ? 'bg-emerald-100 text-emerald-700'
     : contract.status === 'Partial'
       ? 'bg-amber-100 text-amber-700'
       : contract.status === 'Analyzing'
         ? 'bg-blue-100 text-blue-700'
         : 'bg-slate-100 text-slate-600'
   ```
   Amber signals "incomplete but has data" -- consistent with the project's amber=warning color convention.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && grep -n "Partial" src/types/contract.ts && grep -n "bg-amber-100" src/components/ContractCard.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/contract.ts line 184 contains `'Analyzing' | 'Reviewed' | 'Partial' | 'Draft'`
    - src/components/ContractCard.tsx contains `contract.status === 'Partial'`
    - src/components/ContractCard.tsx contains `bg-amber-100 text-amber-700`
    - `npx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>Contract.status type union includes 'Partial' and ContractCard renders an amber badge for Partial status contracts</done>
</task>

<task type="auto">
  <name>Task 2: Update all status filters to include Partial contracts</name>
  <files>src/App.tsx, src/pages/Dashboard.tsx, src/utils/dateUrgency.ts, src/components/PatternsCard.tsx</files>
  <read_first>
    - src/App.tsx (line 38-48 -- deadline count filter for sidebar badge)
    - src/pages/Dashboard.tsx (line 25-30 -- reviewed contracts filter for stats)
    - src/utils/dateUrgency.ts (line 70-80 -- contract filter for timeline entries)
    - src/components/PatternsCard.tsx (line 10-15 -- contract filter for pattern detection)
  </read_first>
  <action>
Update all four `status === 'Reviewed'` filter locations to also include Partial contracts. Partial contracts contain real findings, dates, risk scores, and bid signals -- they should be visible in all data views.

1. **src/App.tsx line 42** -- sidebar urgent deadline badge count:
   Change `.filter(c => c.status === 'Reviewed')`
   to `.filter(c => c.status === 'Reviewed' || c.status === 'Partial')`

2. **src/pages/Dashboard.tsx line 27** -- dashboard stats (risk, findings, etc.):
   Change `.filter((c) => c.status === 'Reviewed')`
   to `.filter((c) => c.status === 'Reviewed' || c.status === 'Partial')`

3. **src/utils/dateUrgency.ts line 75** -- portfolio deadline timeline entries:
   Change `.filter(c => c.status === 'Reviewed')`
   to `.filter(c => c.status === 'Reviewed' || c.status === 'Partial')`

4. **src/components/PatternsCard.tsx line 12** -- pattern detection across contracts:
   Change `.filter((c) => c.status === 'Reviewed')`
   to `.filter((c) => c.status === 'Reviewed' || c.status === 'Partial')`

Do NOT modify src/pages/ContractReview.tsx line 103 -- the `contract.status === 'Analyzing'` check there already handles Partial correctly (Partial !== Analyzing, so it falls to the results branch).
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && grep -n "Partial" src/App.tsx src/pages/Dashboard.tsx src/utils/dateUrgency.ts src/components/PatternsCard.tsx && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `c.status === 'Partial'`
    - src/pages/Dashboard.tsx contains `c.status === 'Partial'`
    - src/utils/dateUrgency.ts contains `c.status === 'Partial'`
    - src/components/PatternsCard.tsx contains `c.status === 'Partial'`
    - Exactly 4 files in src/ contain `status === 'Partial'` filter checks (plus ContractCard badge = 5 total references to 'Partial' in src/)
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>All four filter locations include Partial contracts in data views. Dashboard stats, deadline timeline, sidebar badge, and pattern detection all reflect Partial contract data. Build passes cleanly.</done>
</task>

</tasks>

<verification>
1. Type check: `npx tsc --noEmit` passes with no errors
2. Build: `npm run build` succeeds
3. Grep audit: `grep -rn "=== 'Reviewed'" src/` returns zero results (all locations updated to include Partial)
4. Grep confirm: `grep -rn "'Partial'" src/` returns references in all 6 modified files
</verification>

<success_criteria>
- Contract.status type includes 'Partial' as a union member
- ContractCard shows amber (bg-amber-100 text-amber-700) badge for Partial status
- All 4 filter locations (App.tsx, Dashboard.tsx, dateUrgency.ts, PatternsCard.tsx) include Partial alongside Reviewed
- TypeScript compilation and production build both succeed
- No remaining bare `status === 'Reviewed'` checks in src/ (all updated to include Partial)
</success_criteria>

<output>
After completion, create `.planning/phases/55-partial-status-type-gap-closure/55-01-SUMMARY.md`
</output>
