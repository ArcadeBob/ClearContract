---
phase: 10-industry-trade-knowledge
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/knowledge/registry.ts
  - api/analyze.ts
autonomous: true
requirements:
  - TRADE-01
  - TRADE-02
  - TRADE-03

must_haves:
  truths:
    - "scope-of-work pass receives div08-scope, standards-validation, contract-forms, and ca-title24 modules (4 total, at max capacity)"
    - "risk-overview pass receives contract-forms module"
    - "All three new modules load at runtime via side-effect imports in analyze.ts"
    - "Full application builds and lints without errors"
  artifacts:
    - path: "src/knowledge/registry.ts"
      provides: "Updated PASS_KNOWLEDGE_MAP with all 3 new modules wired to correct passes"
      contains: "div08-scope"
    - path: "api/analyze.ts"
      provides: "Side-effect imports for trade and standards domain index files"
      contains: "knowledge/trade/index"
  key_links:
    - from: "api/analyze.ts"
      to: "src/knowledge/trade/index.ts"
      via: "side-effect import triggers registerModule"
      pattern: "import.*knowledge/trade/index"
    - from: "api/analyze.ts"
      to: "src/knowledge/standards/index.ts"
      via: "side-effect import triggers registerModule"
      pattern: "import.*knowledge/standards/index"
    - from: "src/knowledge/registry.ts"
      to: "src/knowledge/trade/div08-scope.ts"
      via: "PASS_KNOWLEDGE_MAP scope-of-work entry"
      pattern: "scope-of-work.*div08-scope"
    - from: "src/knowledge/registry.ts"
      to: "src/knowledge/standards/contract-forms.ts"
      via: "PASS_KNOWLEDGE_MAP risk-overview and scope-of-work entries"
      pattern: "risk-overview.*contract-forms"
---

<objective>
Wire all three knowledge modules into the analysis pipeline.

Purpose: Connect the modules created in Plan 01 to the correct analysis passes via PASS_KNOWLEDGE_MAP and ensure they load at runtime via side-effect imports in the serverless function entry point.

Output: Updated registry.ts with pass mappings and analyze.ts with domain imports. Full build + lint green.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-industry-trade-knowledge/10-CONTEXT.md
@.planning/phases/10-industry-trade-knowledge/10-01-SUMMARY.md

<interfaces>
<!-- Current state of files to modify -->

From src/knowledge/registry.ts (PASS_KNOWLEDGE_MAP to update):
```typescript
export const PASS_KNOWLEDGE_MAP: Record<string, string[]> = {
  'risk-overview': [],
  'dates-deadlines': [],
  'scope-of-work': ['ca-title24'],
  'legal-indemnification': ['ca-lien-law'],
  'legal-payment-contingency': ['ca-lien-law'],
  'legal-liquidated-damages': [],
  'legal-retainage': [],
  'legal-insurance': [],
  'legal-termination': [],
  'legal-flow-down': [],
  'legal-no-damage-delay': [],
  'legal-lien-rights': ['ca-lien-law'],
  'legal-dispute-resolution': [],
  'legal-change-order': [],
  'verbiage-analysis': [],
  'labor-compliance': ['ca-prevailing-wage', 'ca-calosha'],
};
```

From api/analyze.ts (existing knowledge import):
```typescript
import '../src/knowledge/regulatory/index';
// Add trade and standards imports here
```

Target PASS_KNOWLEDGE_MAP state:
```typescript
'risk-overview': ['contract-forms'],
'scope-of-work': ['ca-title24', 'div08-scope', 'standards-validation', 'contract-forms'],
// All other entries unchanged
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire modules into pass map and add runtime imports</name>
  <files>src/knowledge/registry.ts, api/analyze.ts</files>
  <action>
1. In `src/knowledge/registry.ts`, update PASS_KNOWLEDGE_MAP:
   - Change `'risk-overview': []` to `'risk-overview': ['contract-forms']`
   - Change `'scope-of-work': ['ca-title24']` to `'scope-of-work': ['ca-title24', 'div08-scope', 'standards-validation', 'contract-forms']`
   - All other entries remain unchanged
   - NOTE: scope-of-work now has exactly 4 modules -- this is at the MAX_MODULES_PER_PASS limit. Do not add more.

2. In `api/analyze.ts`, add two side-effect imports alongside the existing regulatory import:
   - `import '../src/knowledge/trade/index';`
   - `import '../src/knowledge/standards/index';`
   - Place these immediately after the existing `import '../src/knowledge/regulatory/index';` line

3. Verify with full build and lint:
   - Run `npm run build` -- must pass (TypeScript compilation + Vite build)
   - Run `npm run lint` -- must pass (ESLint)
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>PASS_KNOWLEDGE_MAP has contract-forms in risk-overview, and div08-scope + standards-validation + contract-forms added to scope-of-work (4 modules total). analyze.ts imports trade/index and standards/index for runtime registration. npm run build and npm run lint both pass cleanly.</done>
</task>

</tasks>

<verification>
- `npm run build` passes -- all TypeScript compiles, Vite bundles successfully
- `npm run lint` passes -- no ESLint errors
- PASS_KNOWLEDGE_MAP scope-of-work has exactly 4 modules: ca-title24, div08-scope, standards-validation, contract-forms
- PASS_KNOWLEDGE_MAP risk-overview has exactly 1 module: contract-forms
- api/analyze.ts has imports for regulatory/index, trade/index, and standards/index
</verification>

<success_criteria>
- Build green, lint green
- All 3 new modules wired to correct passes per CONTEXT.md mapping
- scope-of-work at max capacity (4 modules)
- Runtime imports ensure modules register in serverless function
</success_criteria>

<output>
After completion, create `.planning/phases/10-industry-trade-knowledge/10-02-SUMMARY.md`
</output>
