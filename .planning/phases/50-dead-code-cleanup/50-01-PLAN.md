---
phase: 50-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useContractStore.ts
  - src/hooks/__tests__/useContractStore.test.ts
  - .env.example
  - vite.config.ts
  - CLAUDE.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "isUploading and setIsUploading do not exist anywhere in production or test code"
    - ".env.example contains only valid, non-redundant environment variable entries"
    - "Coverage config references only files that exist on disk"
    - "CLAUDE.md source layout matches actual filesystem (no phantom files)"
    - "Full test suite passes (429 tests) and build succeeds after all removals"
  artifacts:
    - path: "src/hooks/useContractStore.ts"
      provides: "Contract store hook without dead isUploading state"
      contains: "return {"
      not_contains: "isUploading"
    - path: ".env.example"
      provides: "Accurate env documentation with 5 keys"
      not_contains: "^SUPABASE_ANON_KEY"
    - path: "vite.config.ts"
      provides: "Coverage config without phantom file exclusion"
      not_contains: "mockContracts"
    - path: "CLAUDE.md"
      provides: "Source layout matching actual filesystem"
      not_contains: "mockContracts"
  key_links:
    - from: "src/hooks/useContractStore.ts"
      to: "npm run build"
      via: "TypeScript compilation"
      pattern: "build succeeds with no isUploading references"
    - from: "src/hooks/__tests__/useContractStore.test.ts"
      to: "npm run test"
      via: "test suite execution"
      pattern: "429 tests pass (1 fewer than baseline 430)"
---

<objective>
Remove three residual dead code items left from the Supabase migration: orphaned isUploading state in useContractStore, stale SUPABASE_ANON_KEY in .env.example, and phantom mockContracts.ts references in config files.

Purpose: Close out the v2.1 Quality Restoration milestone with a clean codebase -- no dead exports, no misleading env docs, no phantom file references.
Output: Six files edited, one empty directory removed, test suite at 429 tests, build green.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-dead-code-cleanup/50-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove isUploading dead state and fix .env.example</name>
  <files>src/hooks/useContractStore.ts, src/hooks/__tests__/useContractStore.test.ts, .env.example</files>
  <read_first>
    - src/hooks/useContractStore.ts (see current isUploading declaration at line 12 and return object at lines 169-181)
    - src/hooks/__tests__/useContractStore.test.ts (see isUploading test block at lines 330-344)
    - .env.example (see stale SUPABASE_ANON_KEY at line 3)
  </read_first>
  <action>
**CLEAN-01: Remove isUploading from useContractStore.ts**

1. Delete line 12: `const [isUploading, setIsUploading] = useState(false);`
2. In the return object (lines 169-181), remove the two lines:
   - `isUploading,`
   - `setIsUploading,`
   The return object should become:
   ```typescript
   return {
     contracts,
     isLoading,
     error,
     addContract,
     updateContract,
     deleteContract,
     toggleFindingResolved,
     updateFindingNote,
     renameContract,
   };
   ```

3. In `src/hooks/__tests__/useContractStore.test.ts`, delete the entire test block at lines 330-344:
   ```
   it('exposes isUploading and setIsUploading', () => {
     ...
   });
   ```
   This removes exactly 1 test, bringing the suite from 430 to 429.

**CLEAN-02: Fix .env.example**

4. Remove line 3 from `.env.example`: `SUPABASE_ANON_KEY=your_supabase_anon_key`
   The file should contain exactly these 5 lines (in order):
   ```
   ANTHROPIC_API_KEY=your_anthropic_api_key
   SUPABASE_URL=https://your-project-ref.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
   VITE_SUPABASE_URL=https://your-project-ref.supabase.co
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```
  </action>
  <verify>
    <automated>npm run build && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "isUploading\|setIsUploading" src/` returns zero results
    - `grep "^SUPABASE_ANON_KEY" .env.example` returns zero results (no non-VITE-prefixed key)
    - `grep "VITE_SUPABASE_ANON_KEY" .env.example` returns exactly one result
    - `npm run build` exits 0
    - `npm run test -- --run` exits 0 with 429 tests passing
  </acceptance_criteria>
  <done>isUploading/setIsUploading fully removed from hook and tests. .env.example contains only the 5 correct keys with no stale SUPABASE_ANON_KEY entry. Build and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove phantom mockContracts.ts references and empty directory</name>
  <files>vite.config.ts, CLAUDE.md</files>
  <read_first>
    - vite.config.ts (see stale coverage exclusion `'src/data/mockContracts.ts'` at line 35)
    - CLAUDE.md (see phantom `data/mockContracts.ts` entry at line 68 in source layout)
  </read_first>
  <action>
**CLEAN-03: Remove phantom mockContracts.ts references**

1. In `vite.config.ts`, remove line 35: `'src/data/mockContracts.ts',`
   The coverage.exclude array should become:
   ```typescript
   exclude: [
     'src/test/**',
     'api/test-fixtures/**',
     '**/*.test.{ts,tsx}',
     '**/*.d.ts',
     'src/index.tsx',
     'src/vite-env.d.ts',
   ],
   ```

2. In `CLAUDE.md`, remove line 68: `├── data/mockContracts.ts     # Sample contract data (3 contracts at varying risk levels)`
   The source layout tree should go directly from `api/analyzeContract.ts` to `components/`:
   ```
   ├── api/analyzeContract.ts    # Client-side API wrapper (base64 encode + fetch)
   ├── components/               # Reusable UI (SeverityBadge, FindingCard, Sidebar, UploadZone, etc.)
   ```

3. Delete the empty `src/data/` directory: `rmdir src/data` (or `rm -rf src/data` -- it is confirmed empty, mockContracts.ts was deleted in Phase 45).
  </action>
  <verify>
    <automated>npm run test -- --run --coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "mockContracts" vite.config.ts` returns zero results
    - `grep -r "mockContracts" CLAUDE.md` returns zero results
    - `ls src/data/ 2>/dev/null` returns error (directory removed)
    - `npm run test -- --run --coverage` exits 0 with coverage thresholds passing (statements >= 60%, functions >= 60%)
  </acceptance_criteria>
  <done>All phantom mockContracts.ts references removed from vite.config.ts and CLAUDE.md. Empty src/data/ directory deleted. Coverage thresholds still pass.</done>
</task>

</tasks>

<verification>
All three CLEAN requirements verified in one pass:
1. CLEAN-01: `grep -r "isUploading\|setIsUploading" src/` returns empty
2. CLEAN-02: `grep "^SUPABASE_ANON_KEY" .env.example` returns empty; `wc -l .env.example` returns 5
3. CLEAN-03: `grep -r "mockContracts" vite.config.ts CLAUDE.md` returns empty; `ls src/data/` fails
4. Full suite: `npm run build` exits 0, `npm run test -- --run` reports 429 passing tests
</verification>

<success_criteria>
- Zero references to isUploading/setIsUploading in src/
- .env.example has exactly 5 lines with no bare SUPABASE_ANON_KEY
- Zero references to mockContracts in config or documentation files
- src/data/ directory does not exist
- npm run build exits 0
- npm run test -- --run exits 0 (429 tests)
- npm run test -- --run --coverage passes thresholds (statements >= 60%, functions >= 60%)
</success_criteria>

<output>
After completion, create `.planning/phases/50-dead-code-cleanup/50-01-SUMMARY.md`
</output>
