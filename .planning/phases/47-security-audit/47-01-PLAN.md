---
phase: 47-security-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "npm audit reports zero high or critical vulnerabilities"
    - "Full test suite passes (269/269 green) after all dependency changes"
    - "Production build succeeds with no new warnings from upgraded packages"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency versions and npm overrides for transitive vulnerabilities"
      contains: "overrides"
    - path: "package-lock.json"
      provides: "Resolved dependency tree with patched versions"
  key_links:
    - from: "package.json overrides"
      to: "node_modules transitive deps"
      via: "npm install resolution"
      pattern: "overrides"
    - from: "npm audit"
      to: "zero high/critical"
      via: "audit check"
      pattern: "found 0 vulnerabilities|0 high|0 critical"
---

<objective>
Resolve all high and critical npm audit vulnerabilities through non-breaking updates and targeted npm overrides.

Purpose: Eliminate the 13 npm audit vulnerabilities (1 critical, 8 high, 4 moderate) so the project has a clean security posture. SEC-01 requires zero high/critical; moderate vulnerabilities are addressed if safe but not blocking.

Output: Updated package.json (with overrides section), updated package-lock.json, zero high/critical audit findings, 269/269 tests green, clean build.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-security-audit/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run npm audit fix for non-breaking dependency updates</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency versions and structure)
    - .planning/phases/47-security-audit/47-RESEARCH.md (Tier 1 fix details)
  </read_first>
  <action>
Run `npm audit fix` (WITHOUT --force) in the project root. This resolves the non-breaking Tier 1 vulnerabilities:

- jspdf: 4.2.0 -> 4.2.1 (CRITICAL fix)
- flatted: 3.3.3 -> 3.4.2 (HIGH fix, transitive via eslint -> flat-cache)
- tar: 7.5.9 -> 7.5.12 (HIGH fix, transitive via @vercel/nft -> @mapbox/node-pre-gyp)
- undici (direct): 7.22.0 -> 7.24.5+ (HIGH fix, direct dependency)
- @vercel/node: 5.6.9 -> 5.6.18 (semver-compatible update)

Steps:
1. Run `npm audit` to capture the "before" state (save output for SUMMARY)
2. Run `npm audit fix` (no --force flag -- NEVER use --force as it can downgrade packages)
3. Run `npm audit` to see remaining vulnerabilities
4. Run `npm run test` to verify 269/269 pass
5. Run `npm run build` to verify clean build

IMPORTANT: Do NOT use `npm audit fix --force`. The research explicitly warns this can downgrade @vercel/node from 5.6.x to 3.0.1.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npm run test 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` output contains "269 passed" or more with 0 failures
    - `npm run build` exits with code 0
    - `npm audit 2>&1 | grep -c "critical"` returns 0 critical vulnerabilities
    - package.json contains `"jspdf": "^4.2.` (patch update applied)
    - package-lock.json has been updated (git diff shows changes)
  </acceptance_criteria>
  <done>Tier 1 non-breaking fixes applied. jspdf critical resolved. Direct undici, flatted, tar high vulnerabilities resolved. Tests 269/269 green, build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add npm overrides for remaining transitive high vulnerabilities</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (to see current state after Task 1's npm audit fix)
    - .planning/phases/47-security-audit/47-RESEARCH.md (Tier 2 override patterns and pitfalls)
  </read_first>
  <action>
Add an `"overrides"` section to package.json to resolve the remaining HIGH transitive vulnerabilities that npm audit fix cannot reach. Then reinstall and verify.

Add the following overrides to package.json (at the top level, sibling to "dependencies"):

```json
"overrides": {
  "@vercel/node": {
    "undici": "^5.29.0"
  },
  "@vercel/build-utils": {
    "@vercel/python-analysis": {
      "minimatch": "^10.3.0"
    }
  },
  "@vercel/node>path-to-regexp": "^6.3.0"
}
```

Override details:
- **undici nested** (HIGH): @vercel/node ships undici@5.28.4. Override to ^5.29.0 (stays on 5.x to avoid breaking @vercel/node's internal HTTP client). If no 5.29.x exists that fixes the CVEs, try ^6.22.0 but test carefully.
- **path-to-regexp** (HIGH): @vercel/node uses 6.1.0. Override to ^6.3.0 which patches the ReDoS vulnerability.
- **minimatch** (HIGH): @vercel/build-utils -> @vercel/python-analysis uses vulnerable 10.x. Override to ^10.3.0.

Do NOT override esbuild (moderate, dev-only, breaks vite -- see Pitfall 4 in research).

For moderate ajv vulnerability: attempt adding `"@vercel/static-config>ajv": "^8.17.2"` to overrides ONLY if it doesn't break build/tests. If it causes issues, remove it -- moderates are not required for SEC-01.

Steps:
1. Read package.json to see current state after Task 1
2. Add the overrides section to package.json
3. Run `rm -rf node_modules package-lock.json && npm install` (required for overrides to take effect -- see Pitfall 3 in research)
4. Run `npm audit --audit-level=high` to verify zero high/critical remain
5. Run `npm ls undici` to verify the override resolved in @vercel/node subtree
6. Run `npm ls path-to-regexp` to verify 6.3.0+ in @vercel/node subtree
7. Run `npm ls minimatch` to verify 10.3.0+ in @vercel/python-analysis subtree
8. Run `npm run test` to verify 269/269 pass (SEC-02)
9. Run `npm run build` to verify clean build
10. Run `npm audit` to get final full audit output for SUMMARY

If any override breaks tests or build:
- Remove the problematic override
- Re-run `rm -rf node_modules package-lock.json && npm install`
- Document the unresolvable vulnerability in the SUMMARY as a known limitation of the parent package
- SEC-01 requires zero HIGH/CRITICAL -- if a high vuln cannot be overridden safely, document why and flag as a blocker

Final state: `npm audit --audit-level=high` should report 0 high/critical vulnerabilities. Some moderate vulnerabilities may remain (acceptable per SEC-01 scope).
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npm audit --audit-level=high 2>&1 && npm run test 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"overrides"` key at root level
    - package.json overrides section contains `"undici"` entry under `"@vercel/node"`
    - package.json overrides section contains `"path-to-regexp"` reference
    - package.json overrides section contains `"minimatch"` reference
    - `npm audit --audit-level=high` exits with code 0 (no high or critical findings)
    - `npm run test` output contains "269 passed" or more with 0 failures
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All high and critical npm audit vulnerabilities resolved. npm overrides in place for transitive deps. Tests 269/269 green. Build clean. SEC-01 and SEC-02 satisfied.</done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. `npm audit` -- zero high or critical vulnerabilities (SEC-01)
2. `npm run test` -- 269/269 pass (SEC-02)
3. `npm run build` -- exits 0 with no new warnings
4. package.json has overrides section with scoped transitive dep fixes
5. No `--force` was used, no packages were downgraded
</verification>

<success_criteria>
- SEC-01: `npm audit --audit-level=high` exits 0 (zero high/critical vulnerabilities)
- SEC-02: `npm run test` shows 269/269 (or more) tests passing with 0 failures
- Production build (`npm run build`) succeeds cleanly
- All changes are in package.json and package-lock.json only -- no source code modifications
</success_criteria>

<output>
After completion, create `.planning/phases/47-security-audit/47-01-SUMMARY.md`
</output>
