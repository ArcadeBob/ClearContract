---
phase: 49-coverage-push
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/exportContractCsv.test.ts
  - src/utils/exportContractPdf.test.ts
  - src/utils/settingsValidation.test.ts
  - src/utils/palette.test.ts
  - src/hooks/__tests__/useContractStore.test.ts
autonomous: true
requirements: [COV-02, COV-03]

must_haves:
  truths:
    - "exportContractCsv produces valid CSV with headers and escaped content"
    - "exportContractPdf generates PDF via jsPDF mock without errors"
    - "settingsValidation validates dollar, date, and employee count formats"
    - "palette returns color strings for known severity values"
    - "useContractStore key mutations (addContract, updateContract, deleteContract, navigateTo) work with mocked Supabase"
  artifacts:
    - path: "src/utils/exportContractCsv.test.ts"
      provides: "CSV export function tests"
      min_lines: 25
    - path: "src/utils/exportContractPdf.test.ts"
      provides: "PDF export function tests with jsPDF mock"
      min_lines: 25
    - path: "src/utils/settingsValidation.test.ts"
      provides: "Settings validation function tests"
      min_lines: 25
    - path: "src/utils/palette.test.ts"
      provides: "Palette utility tests"
      min_lines: 10
    - path: "src/hooks/__tests__/useContractStore.test.ts"
      provides: "Contract store hook mutation tests"
      min_lines: 50
  key_links:
    - from: "src/utils/exportContractCsv.test.ts"
      to: "src/utils/exportContractCsv.ts"
      via: "direct import of exported functions"
      pattern: "import.*exportContractCsv"
    - from: "src/hooks/__tests__/useContractStore.test.ts"
      to: "src/hooks/useContractStore.ts"
      via: "renderHook with mocked Supabase and useToast"
      pattern: "renderHook.*useContractStore"
---

<objective>
Test utility functions (CSV/PDF export, settings validation, palette) and useContractStore hook mutations to cover ~600 statements of previously uncovered code.

Purpose: Utilities are pure functions with well-defined I/O -- high coverage per test effort. useContractStore (182 LOC at 0%) is the app's central state hook and covers many function exports.
Output: 5 test files covering all untested utilities and the contract store hook.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-coverage-push/49-CONTEXT.md
@.planning/phases/49-coverage-push/49-RESEARCH.md

<interfaces>
<!-- Key exports the executor needs to test -->

From src/utils/exportContractCsv.ts (read file for full exports):
- Functions: escapeCsv, sanitizeFilename, exportContractCsv
- exportContractCsv takes a Contract and produces CSV string or triggers download

From src/utils/exportContractPdf.ts (read file for full exports):
- Function: exportContractPdf
- Uses jsPDF and jspdf-autotable -- both must be mocked

From src/utils/settingsValidation.ts (read file for full exports):
- Function: validateField(value: string, type: string) -> { valid: boolean, formatted?: string, error?: string }
- Types: 'dollar', 'date', 'employeeCount', 'text', etc.

From src/utils/palette.ts (read file for full exports):
- Functions mapping severity/category to Tailwind color classes

From src/hooks/useContractStore.ts (read file for full exports):
- Hook: useContractStore() returns { contracts, activeContract, activeView, isLoading, error, addContract, updateContract, deleteContract, navigateTo, ... }
- Depends on: supabase client (from ../lib/supabase), useToast (from ./useToast)
- Mock pattern: vi.mock('../lib/supabase') with createTableMock, vi.mock('./useToast')

From src/test/factories.ts:
- createContract(), createFinding(), createContractDate() -- Zod-validated factories
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utility function tests (CSV, PDF, settings, palette)</name>
  <files>src/utils/exportContractCsv.test.ts, src/utils/exportContractPdf.test.ts, src/utils/settingsValidation.test.ts, src/utils/palette.test.ts</files>
  <read_first>
    - src/utils/exportContractCsv.ts
    - src/utils/exportContractPdf.ts
    - src/utils/settingsValidation.ts
    - src/utils/palette.ts
    - src/test/factories.ts (for createContract, createFinding)
    - src/utils/bidSignal.test.ts (existing utility test pattern)
    - src/utils/errors.test.ts (existing utility test pattern)
  </read_first>
  <action>
Create `src/utils/exportContractCsv.test.ts`:

1. Import functions from `./exportContractCsv` -- read the file first to see exact exports (likely `escapeCsv`, `sanitizeFilename`, and the main export function)
2. Import `createContract`, `createFinding` from `../test/factories`
3. Mock `URL.createObjectURL` and `document.createElement` / click behavior for download trigger (the function likely creates a Blob + triggers download)
4. Test `escapeCsv`: wraps strings containing commas in quotes, escapes internal quotes by doubling
5. Test `sanitizeFilename`: removes special characters, produces safe filename
6. Test main export function: call with a contract that has findings, verify the CSV string (or Blob content) contains header row and finding data. Use `vi.spyOn(document, 'createElement')` to intercept the download anchor
7. Test with empty findings array: should still produce headers

Create `src/utils/exportContractPdf.test.ts`:

1. Mock `jspdf` module: `vi.mock('jspdf', () => ({ default: vi.fn(() => mockJsPdfInstance) }))` where `mockJsPdfInstance` has `text: vi.fn()`, `setFontSize: vi.fn()`, `setTextColor: vi.fn()`, `save: vi.fn()`, `internal: { pageSize: { getWidth: () => 210 } }`, `getStringUnitWidth: vi.fn(() => 10)`, `getFontSize: vi.fn(() => 12)`, `splitTextToSize: vi.fn((text: string) => [text])`
2. Mock `jspdf-autotable`: `vi.mock('jspdf-autotable', () => ({ default: vi.fn() }))`
3. Import `createContract`, `createFinding` from `../test/factories`
4. Read the actual file first to see exact function signature and what jsPDF methods it calls
5. Test happy path: call exportContractPdf with a contract containing findings, assert `save` was called with a filename containing the contract name
6. Test with contract with no findings: should still produce a PDF (save called)

Create `src/utils/settingsValidation.test.ts`:

1. Import `validateField` (and any other exports) from `./settingsValidation`
2. Read the file first to see exact function signatures and supported types
3. Test dollar validation: `validateField('$1,000,000', 'dollar')` returns `{ valid: true, ... }`
4. Test invalid dollar: `validateField('abc', 'dollar')` returns `{ valid: false, ... }`
5. Test date validation: `validateField('2027-01-01', 'date')` returns valid
6. Test invalid date: `validateField('not-a-date', 'date')` returns invalid
7. Test employee count range: `validateField('15-25', 'employeeCount')` returns valid
8. Test empty string: returns valid (empty is allowed)
9. Test each validation type the file supports -- read the source to find all type branches

Create `src/utils/palette.test.ts`:

1. Import all exports from `./palette`
2. Read the file first to see exact exports (likely severity-to-color mapping functions)
3. Test each exported function with known severity values (Critical, High, Medium, Low, Info)
4. Test with unknown/fallback value
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx vitest run src/utils/exportContractCsv.test.ts src/utils/exportContractPdf.test.ts src/utils/settingsValidation.test.ts src/utils/palette.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/exportContractCsv.test.ts imports from `./exportContractCsv` and `../test/factories`
    - src/utils/exportContractPdf.test.ts contains `vi.mock('jspdf'`
    - src/utils/settingsValidation.test.ts contains `validateField` tests for at least dollar, date, and employeeCount types
    - src/utils/palette.test.ts imports from `./palette` and tests at least 3 severity values
    - All 4 test files pass (vitest exit code 0)
  </acceptance_criteria>
  <done>All 4 utility test files created and passing. CSV export, PDF export (with jsPDF mock), settings validation, and palette functions all covered.</done>
</task>

<task type="auto">
  <name>Task 2: useContractStore hook mutation tests</name>
  <files>src/hooks/__tests__/useContractStore.test.ts</files>
  <read_first>
    - src/hooks/useContractStore.ts
    - src/hooks/__tests__/useContractFiltering.test.ts (existing hook test pattern)
    - src/contexts/AuthContext.test.tsx (Supabase mock pattern)
    - src/test/factories.ts (createContract factory)
    - src/lib/supabase.ts (module to mock)
    - src/hooks/useToast.ts (module to mock)
  </read_first>
  <action>
Create `src/hooks/__tests__/useContractStore.test.ts`:

1. Set up module-level mocks BEFORE imports:
   ```typescript
   import { vi, describe, it, expect, beforeEach } from 'vitest';

   // Mock Supabase
   const mockFrom = vi.fn();
   vi.mock('../../lib/supabase', () => ({
     supabase: { from: mockFrom },
   }));

   // Mock useToast
   const mockShowToast = vi.fn();
   vi.mock('../useToast', () => ({
     useToast: () => ({ showToast: mockShowToast }),
   }));

   // Mock useAuth
   vi.mock('../../contexts/AuthContext', () => ({
     useAuth: () => ({ user: { id: 'test-user-id' }, session: { access_token: 'test-token' } }),
   }));
   ```
2. Import `renderHook`, `act`, `waitFor` from `@testing-library/react`
3. Import `useContractStore` from the hook file
4. Import `createContract` from `../../test/factories`
5. Read the actual hook file to understand the exact return shape and function signatures

6. In `beforeEach`: reset all mocks, configure `mockFrom` to return a Supabase query builder mock that resolves to `{ data: [], error: null }` for the initial fetch. Use the `createTableMock` pattern -- read the Phase 46 test files to see how it was done, or build a simple chainable mock:
   ```typescript
   const createChainMock = (data: unknown[] = []) => ({
     select: vi.fn().mockReturnThis(),
     eq: vi.fn().mockReturnThis(),
     insert: vi.fn().mockReturnThis(),
     update: vi.fn().mockReturnThis(),
     delete: vi.fn().mockReturnThis(),
     single: vi.fn().mockResolvedValue({ data: data[0] ?? null, error: null }),
     then: vi.fn((cb: (val: { data: unknown[]; error: null }) => void) => cb({ data, error: null })),
     order: vi.fn().mockReturnThis(),
   });
   ```

7. Test "navigateTo changes active view": renderHook, call `result.current.navigateTo('settings')`, assert `result.current.activeView === 'settings'`

8. Test "addContract adds to contracts array": set up mockFrom to return a chain mock that resolves insert with a new contract, call `result.current.addContract(contractData)`, use `waitFor` to assert the contract appears in `result.current.contracts`

9. Test "updateContract modifies existing contract": start with one contract in state (from initial fetch mock), call updateContract with modified fields, use `waitFor` to assert the contract is updated in `result.current.contracts`

10. Test "deleteContract removes from contracts array": start with one contract, call deleteContract with its id, use `waitFor` to assert `result.current.contracts` length is 0

Note: The exact mock setup depends on reading the hook file -- the hook may use different Supabase query patterns. Read `useContractStore.ts` carefully and match the mock to the actual query chains used (e.g., `.from('contracts').select('*').eq('user_id', ...)` etc.)
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx vitest run src/hooks/__tests__/useContractStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/__tests__/useContractStore.test.ts contains `vi.mock('../../lib/supabase'`
    - src/hooks/__tests__/useContractStore.test.ts contains `vi.mock('../useToast'`
    - src/hooks/__tests__/useContractStore.test.ts contains `renderHook` call with `useContractStore`
    - src/hooks/__tests__/useContractStore.test.ts tests navigateTo, addContract, updateContract, and deleteContract
    - All tests pass (vitest exit code 0)
  </acceptance_criteria>
  <done>useContractStore hook tested for 4 key mutations (navigateTo, addContract, updateContract, deleteContract) with mocked Supabase and useToast.</done>
</task>

</tasks>

<verification>
All 5 test files exist and pass:
```bash
npx vitest run src/utils/exportContractCsv.test.ts src/utils/exportContractPdf.test.ts src/utils/settingsValidation.test.ts src/utils/palette.test.ts src/hooks/__tests__/useContractStore.test.ts
```
</verification>

<success_criteria>
- All 4 utility functions tested with happy path and at least one error case each
- jsPDF properly mocked for PDF export tests (no jsdom errors)
- useContractStore tested for 4 key mutations per user decision
- All new tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/49-coverage-push/49-02-SUMMARY.md`
</output>
