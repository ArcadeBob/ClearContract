---
phase: 49-coverage-push
plan: 03
type: execute
wave: 2
depends_on: [49-01, 49-02]
files_modified:
  - src/components/StatCard.test.tsx
  - src/components/ActionPriorityBadge.test.tsx
  - src/components/AnalysisProgress.test.tsx
  - src/components/CategoryFilter.test.tsx
  - src/components/CategorySection.test.tsx
  - src/components/ClauseQuote.test.tsx
  - src/components/ConfirmDialog.test.tsx
  - src/components/ContractCard.test.tsx
  - src/components/DateTimeline.test.tsx
  - src/components/LoadingScreen.test.tsx
  - src/components/MultiSelectDropdown.test.tsx
  - src/components/NegotiationChecklist.test.tsx
  - src/components/ReviewHeader.test.tsx
  - src/components/RiskScoreDisplay.test.tsx
  - src/components/RiskSummary.test.tsx
  - src/components/Toast.test.tsx
  - src/components/LegalMetaBadge/__tests__/badges.test.tsx
  - src/components/ScopeMetaBadge/__tests__/badges.test.tsx
  - src/pages/Dashboard.test.tsx
  - src/pages/AllContracts.test.tsx
  - src/pages/ContractUpload.test.tsx
  - src/pages/Settings.test.tsx
autonomous: true
requirements: [COV-01, COV-02, COV-03]

must_haves:
  truths:
    - "npm run test:coverage exits 0 with statements >= 60% and functions >= 60%"
    - "Previously untested components render without errors in test environment"
    - "Pages render with mocked providers without errors"
    - "MetaBadge components render with factory-generated findings"
  artifacts:
    - path: "src/components/StatCard.test.tsx"
      provides: "StatCard render test"
      min_lines: 8
    - path: "src/components/LegalMetaBadge/__tests__/badges.test.tsx"
      provides: "Parameterized tests for all 11 LegalMetaBadge components"
      min_lines: 40
    - path: "src/components/ScopeMetaBadge/__tests__/badges.test.tsx"
      provides: "Parameterized tests for all 4 ScopeMetaBadge components"
      min_lines: 20
    - path: "src/pages/Dashboard.test.tsx"
      provides: "Dashboard page shallow render test"
      min_lines: 15
  key_links:
    - from: "src/components/LegalMetaBadge/__tests__/badges.test.tsx"
      to: "src/components/LegalMetaBadge/*.tsx"
      via: "import each badge component, render with pass-specific factory findings"
      pattern: "import.*Badge.*from"
    - from: "src/pages/Dashboard.test.tsx"
      to: "src/hooks/useContractStore.ts"
      via: "vi.mock of useContractStore returning minimal state"
      pattern: "vi.mock.*useContractStore"
---

<objective>
Add shallow render tests for untested components and pages, plus MetaBadge parameterized tests, to push coverage past the 65% statement / 65% function target (5% buffer over CI threshold).

Purpose: Components and pages represent ~2400+ LOC at 0-9% coverage. Simple render tests (mount, check key element exists) cover the component function body plus JSX returns with minimal effort. This plan runs after 49-01 and 49-02 so we can measure the remaining gap and know exactly how many components are needed.
Output: ~20 test files covering all major untested components and 4 pages, verified by `npx vitest run --coverage` passing the 60% threshold.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-coverage-push/49-CONTEXT.md
@.planning/phases/49-coverage-push/49-RESEARCH.md
@.planning/phases/49-coverage-push/49-01-SUMMARY.md
@.planning/phases/49-coverage-push/49-02-SUMMARY.md

<interfaces>
<!-- Established test patterns the executor must follow -->

From src/test/render.tsx:
```typescript
export { customRender as render };
export { screen, within, waitFor } from '@testing-library/react';
export { default as userEvent } from '@testing-library/user-event';
```

From src/test/factories.ts (key factories to use):
- createContract() -- returns Contract with sensible defaults
- createFinding() -- returns Finding with sensible defaults
- createContractDate() -- returns ContractDate
- createIndemnificationFinding(), createPaymentContingencyFinding(), etc. -- 15 pass-specific finding factories

From src/components/FindingCard.test.tsx (PATTERN TO FOLLOW):
```typescript
import { render, screen } from '../test/render';
import { createFinding } from '../test/factories';
import { FindingCard } from './FindingCard';

describe('FindingCard', () => {
  it('renders all sections for a fully-loaded finding', () => {
    const finding = createFinding({ title: 'Test', ... });
    render(<FindingCard finding={finding} index={0} />);
    expect(screen.getByText('Test')).toBeInTheDocument();
  });
});
```

From src/pages/LoginPage.test.tsx (PAGE TEST PATTERN):
```typescript
vi.mock('../hooks/useContractStore', () => ({
  useContractStore: () => ({ contracts: [], isLoading: false, ... }),
}));
vi.mock('../contexts/AuthContext', () => ({
  useAuth: () => ({ user: null, session: null, signOut: vi.fn() }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component render tests and MetaBadge parameterized tests</name>
  <files>
    src/components/StatCard.test.tsx,
    src/components/ActionPriorityBadge.test.tsx,
    src/components/AnalysisProgress.test.tsx,
    src/components/CategoryFilter.test.tsx,
    src/components/CategorySection.test.tsx,
    src/components/ClauseQuote.test.tsx,
    src/components/ConfirmDialog.test.tsx,
    src/components/ContractCard.test.tsx,
    src/components/DateTimeline.test.tsx,
    src/components/LoadingScreen.test.tsx,
    src/components/MultiSelectDropdown.test.tsx,
    src/components/NegotiationChecklist.test.tsx,
    src/components/ReviewHeader.test.tsx,
    src/components/RiskScoreDisplay.test.tsx,
    src/components/RiskSummary.test.tsx,
    src/components/Toast.test.tsx,
    src/components/LegalMetaBadge/__tests__/badges.test.tsx,
    src/components/ScopeMetaBadge/__tests__/badges.test.tsx
  </files>
  <read_first>
    - src/components/StatCard.tsx
    - src/components/ActionPriorityBadge.tsx
    - src/components/AnalysisProgress.tsx
    - src/components/CategoryFilter.tsx
    - src/components/CategorySection.tsx
    - src/components/ClauseQuote.tsx
    - src/components/ConfirmDialog.tsx
    - src/components/ContractCard.tsx
    - src/components/DateTimeline.tsx
    - src/components/LoadingScreen.tsx
    - src/components/RiskScoreDisplay.tsx
    - src/components/RiskSummary.tsx
    - src/components/Toast.tsx
    - src/components/LegalMetaBadge/index.tsx (barrel export to see all badge names)
    - src/components/LegalMetaBadge/IndemnificationBadge.tsx (one badge to see props pattern)
    - src/components/LegalMetaBadge/shared.ts
    - src/components/ScopeMetaBadge/index.tsx
    - src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx (one badge to see props pattern)
    - src/components/ScopeMetaBadge/shared.ts
    - src/test/factories.ts (all factory functions available)
    - src/test/render.tsx
    - src/components/FindingCard.test.tsx (pattern to follow)
    - src/types/contract.ts (for type definitions needed by props)
  </read_first>
  <action>
For EACH untested component, create a test file following this exact pattern:

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '../test/render';
// Import the component
// Import factories as needed

describe('ComponentName', () => {
  it('renders without error', () => {
    render(<ComponentName {...minimalProps} />);
    expect(screen.getByText('expected text')).toBeInTheDocument();
  });
});
```

**Read each component file first** to determine:
- What props are required (check the Props type/interface)
- What text or role elements are rendered (to assert against)
- What child components or hooks it uses (may need additional mocks)

Specific component notes:

**StatCard.tsx**: Likely takes `label`, `value`, `icon` props. Render with `label="Total" value={42}` and a Lucide icon (import `FileText` from `lucide-react`). Assert label and value text.

**ActionPriorityBadge.tsx**: Takes `priority` prop. Render with `priority="pre-bid"`. Assert rendered text.

**AnalysisProgress.tsx**: May take progress-related props. Read first.

**CategoryFilter.tsx**: Takes categories and onFilter handler. Use mock categories from contract types.

**CategorySection.tsx**: Takes category name and findings array. Use `createFinding()`.

**ClauseQuote.tsx**: Takes `text` and possibly `reference`. Render and assert text appears.

**ConfirmDialog.tsx**: Takes `isOpen`, `title`, `message`, `onConfirm`, `onCancel`. Render with `isOpen={true}` and assert title/message visible.

**ContractCard.tsx**: Takes a `contract` prop. Use `createContract()`.

**DateTimeline.tsx**: Takes dates array. Use `createContractDate()`.

**LoadingScreen.tsx**: Likely takes no props or a `message` prop. Render and assert loading indicator.

**MultiSelectDropdown.tsx**: Takes options, selected, onChange. Read props first.

**NegotiationChecklist.tsx**: Takes findings array. Use `createFinding()`.

**ReviewHeader.tsx**: Takes contract prop. Use `createContract()`.

**RiskScoreDisplay.tsx**: Takes `score` number prop. Render with `score={75}` and assert score text.

**RiskSummary.tsx**: Takes contract or findings. Use factory data.

**Toast.tsx**: Takes message and type props. Render and assert message.

**LegalMetaBadge parameterized tests** (`src/components/LegalMetaBadge/__tests__/badges.test.tsx`):
1. Import all 11 badge components from the barrel export (`../index.tsx`)
2. Import the corresponding pass-specific finding factories from `../../../test/factories`
3. Create a `BADGES` array mapping each badge to its factory function:
   ```
   { Badge: IndemnificationBadge, factory: createIndemnificationFinding, name: 'IndemnificationBadge' },
   { Badge: InsuranceBadge, factory: createInsuranceFinding, name: 'InsuranceBadge' },
   // ... all 11
   ```
4. Use `describe.each(BADGES)` to test each: render `<Badge finding={factory()} />`, assert no crash and at least one element renders (use `container.firstChild` !== null as minimum check)

**ScopeMetaBadge parameterized tests** (`src/components/ScopeMetaBadge/__tests__/badges.test.tsx`):
Same pattern as LegalMetaBadge but for 4 scope badges: ScopeOfWorkBadge, DatesDeadlinesBadge, LaborComplianceBadge, VerbiageBadge.

**Important:** If any component imports hooks like `useContractStore` or `useAuth`, add `vi.mock` at the top of that test file (before component import) returning minimal state. Follow the LoginPage.test.tsx pattern shown in interfaces.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx vitest run src/components/StatCard.test.tsx src/components/ActionPriorityBadge.test.tsx src/components/LegalMetaBadge/__tests__/badges.test.tsx src/components/ScopeMetaBadge/__tests__/badges.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StatCard.test.tsx exists and contains `render(<StatCard`
    - src/components/LegalMetaBadge/__tests__/badges.test.tsx contains `describe.each` and imports all 11 badge components
    - src/components/ScopeMetaBadge/__tests__/badges.test.tsx contains `describe.each` and imports all 4 badge components
    - At least 16 new component test files created (some small components may be grouped)
    - All component tests pass (vitest exit code 0)
  </acceptance_criteria>
  <done>All 19 untested components have render tests. 11 LegalMetaBadge and 4 ScopeMetaBadge components covered via parameterized tests. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Page shallow render tests and coverage gate verification</name>
  <files>
    src/pages/Dashboard.test.tsx,
    src/pages/AllContracts.test.tsx,
    src/pages/ContractUpload.test.tsx,
    src/pages/Settings.test.tsx
  </files>
  <read_first>
    - src/pages/Dashboard.tsx
    - src/pages/AllContracts.tsx
    - src/pages/ContractUpload.tsx
    - src/pages/Settings.tsx
    - src/pages/LoginPage.test.tsx (existing page test pattern to follow)
    - src/hooks/useContractStore.ts (return shape to mock)
    - src/contexts/AuthContext.tsx (useAuth shape to mock)
    - src/test/factories.ts
    - src/test/render.tsx
  </read_first>
  <action>
For each page, create a shallow render test that:
1. Mocks ALL hooks the page imports (useContractStore, useAuth, useRouter if applicable, useContractFiltering if applicable)
2. Renders the page component
3. Asserts at least one visible element renders

**Read each page file first** to identify all imported hooks that need mocking.

Create `src/pages/Dashboard.test.tsx`:
1. Mock useContractStore at module level returning `{ contracts: [], activeContract: null, activeView: 'dashboard', isLoading: false, error: null, navigateTo: vi.fn() }` -- read Dashboard.tsx to see exact destructured properties
2. Mock useAuth returning `{ user: { id: 'test', email: 'test@test.com' }, session: { access_token: 'tok' }, signOut: vi.fn() }`
3. Render `<Dashboard />`, assert page heading or key text is in document

Create `src/pages/AllContracts.test.tsx`:
1. Mock useContractStore returning `{ contracts: [], isLoading: false, navigateTo: vi.fn(), deleteContract: vi.fn() }` -- read file for exact props
2. Mock useAuth same as above
3. Mock useContractFiltering if imported (return `{ filtered: [], filters: {}, setFilter: vi.fn() }` -- read the hook to see return shape)
4. Render `<AllContracts />`, assert page renders

Create `src/pages/ContractUpload.test.tsx`:
1. Mock useContractStore, useAuth as above
2. Render `<ContractUpload />`, assert upload-related text is visible

Create `src/pages/Settings.test.tsx`:
1. Mock useContractStore, useAuth as above
2. Render `<Settings />`, assert settings heading renders

**Note on ContractReview.tsx and ContractComparison.tsx:** These are complex pages with many dependencies. Read them first -- if they can be shallow-rendered with mocks in <5 minutes of setup, include them. If they require extensive prop setup or multiple sub-component mocks, skip them (we should have enough coverage from the other tests). The user said "pages get shallow render-only tests" so keep it simple.

**Final step -- run full coverage check:**
```bash
npx vitest run --coverage
```
Verify output shows statements >= 60% and functions >= 60%. If still below, identify the top 1-2 uncovered files and add a quick test. The research estimates knowledge + utils + components + pages should push well past 65%.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx vitest run --coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Dashboard.test.tsx contains `vi.mock` for useContractStore and renders Dashboard
    - src/pages/AllContracts.test.tsx contains `vi.mock` for useContractStore and renders AllContracts
    - src/pages/ContractUpload.test.tsx contains render test
    - src/pages/Settings.test.tsx contains render test
    - `npx vitest run --coverage` exits with code 0 (thresholds met)
    - Coverage output shows statements >= 60% and functions >= 60%
  </acceptance_criteria>
  <done>All 4 page shallow render tests pass. Full coverage suite passes with statements >= 60% and functions >= 60%, meeting CI threshold with buffer.</done>
</task>

</tasks>

<verification>
Full coverage gate:
```bash
cd C:/Users/relmq/projects/clearcontract && npx vitest run --coverage
```
Must exit 0 (all tests pass AND coverage thresholds met).
</verification>

<success_criteria>
- All untested components have at least a render test
- All MetaBadge components (11 legal + 4 scope) tested via parameterized tests
- 4 page components have shallow render tests with mocked providers
- `npx vitest run --coverage` exits 0 with statements >= 60% and functions >= 60%
- CI pipeline would pass the coverage threshold check
</success_criteria>

<output>
After completion, create `.planning/phases/49-coverage-push/49-03-SUMMARY.md`
</output>
