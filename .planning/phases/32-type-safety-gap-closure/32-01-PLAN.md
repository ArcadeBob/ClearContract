---
phase: 32-type-safety-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useContractStore.ts
  - src/components/CoverageComparisonTab.tsx
autonomous: true
gap_closure: true
requirements:
  - TYPE-01

must_haves:
  truths:
    - "tsc --noEmit produces zero errors across the entire codebase"
    - "npm run build succeeds with exit code 0"
    - "updateFindingNote correctly handles undefined note by coalescing to empty string"
  artifacts:
    - path: "src/hooks/useContractStore.ts"
      provides: "Fixed updateFindingNote with note ?? '' coalesce"
      contains: "note: note ?? ''"
    - path: "src/components/CoverageComparisonTab.tsx"
      provides: "Removed unused loop variable"
      contains: "{allRows.map((row) => {"
  key_links:
    - from: "src/hooks/useContractStore.ts"
      to: "src/schemas/finding.ts"
      via: "Finding type requires note: string (not optional)"
      pattern: "note: note \\?\\? ''"
---

<objective>
Fix two TypeScript errors that are the last remaining TYPE-01 gap from the v1.5 milestone audit: a TS2322 type mismatch in updateFindingNote and a TS6133 unused variable in CoverageComparisonTab.

Purpose: Close the final TYPE-01 requirement so v1.5 Code Health milestone can ship clean.
Output: Two files with one-line fixes each; tsc --noEmit passes with zero errors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-type-safety-gap-closure/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix updateFindingNote TS2322 and CoverageComparisonTab TS6133</name>
  <files>src/hooks/useContractStore.ts, src/components/CoverageComparisonTab.tsx</files>
  <read_first>
    - src/hooks/useContractStore.ts (see line 53-61, the updateFindingNote function and its spread)
    - src/components/CoverageComparisonTab.tsx (see line 171, the allRows.map callback)
    - src/schemas/finding.ts (confirm Finding.note is z.string() required, not optional)
  </read_first>
  <action>
Two targeted one-line fixes:

**Fix 1 -- useContractStore.ts line 60:**
Change the spread inside the .map from:
```typescript
f.id === findingId ? { ...f, note } : f
```
to:
```typescript
f.id === findingId ? { ...f, note: note ?? '' } : f
```
This coalesces `undefined` to empty string `''` at the store level, satisfying `Finding.note: string` (required by MergedFindingSchema's `z.string()`). The parameter type `note: string | undefined` on line 53 stays unchanged -- callers like FindingCard.tsx pass `undefined` to mean "delete note", and this coalesce normalizes that to `''`.

Do NOT change the parameter type. Do NOT change the Zod schema. Do NOT touch any other file.

**Fix 2 -- CoverageComparisonTab.tsx line 171:**
Change:
```typescript
{allRows.map((row, i) => {
```
to:
```typescript
{allRows.map((row) => {
```
Remove the unused `i` parameter entirely (it is never referenced in the callback body).
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx tsc --noEmit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useContractStore.ts contains the exact string `note: note ?? ''`
    - src/hooks/useContractStore.ts line 53 still has parameter `note: string | undefined` (unchanged)
    - src/components/CoverageComparisonTab.tsx contains `{allRows.map((row) => {` (no second parameter)
    - `npx tsc --noEmit` exits with code 0 and produces no error output
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Both TS errors eliminated. tsc --noEmit passes clean. npm run build succeeds.</done>
</task>

</tasks>

<verification>
```bash
# Verify zero type errors
npx tsc --noEmit

# Verify production build
npm run build

# Verify the specific fixes are in place
grep -n "note: note ?? ''" src/hooks/useContractStore.ts
grep -n "allRows.map((row)" src/components/CoverageComparisonTab.tsx
```
</verification>

<success_criteria>
1. `npx tsc --noEmit` exits 0 with no TS2322 or TS6133 errors
2. `npm run build` exits 0
3. useContractStore.ts contains `note: note ?? ''` on the spread line
4. CoverageComparisonTab.tsx map callback has only `(row)` parameter
</success_criteria>

<output>
After completion, create `.planning/phases/32-type-safety-gap-closure/32-01-SUMMARY.md`
</output>
