---
phase: 30-type-safety-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/contract.ts
  - src/storage/contractStorage.ts
autonomous: true
gap_closure: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "tsc --noEmit passes with zero phase-30-introduced errors"
    - "Finding type is usable as a local name inside contract.ts (Contract interface compiles)"
    - "migrateContracts() cast satisfies strict TypeScript without runtime behavior change"
  artifacts:
    - path: "src/types/contract.ts"
      provides: "Finding type import + re-export for local and external use"
      contains: "import type.*MergedFinding as Finding"
    - path: "src/storage/contractStorage.ts"
      provides: "Type-safe migration cast"
      contains: "as unknown as Contract"
  key_links:
    - from: "src/types/contract.ts"
      to: "src/schemas/finding.ts"
      via: "import type + export type"
      pattern: "import type.*MergedFinding.*from.*schemas/finding"
---

<objective>
Fix two tsc --noEmit errors introduced by phase 30 plans that prevent strict TypeScript compilation.

Purpose: Phase 30 achieved its goals at the Vite build level but introduced two TypeScript strict-mode errors (TS2304 and TS2352). These one-line fixes complete TYPE-01 (Zod/TS agreement) by ensuring the wiring compiles under tsc --noEmit.

Output: Two files patched, tsc --noEmit passes clean for phase-30 code.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-type-safety-hardening/30-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Finding type resolution in contract.ts</name>
  <files>src/types/contract.ts</files>
  <action>
At the top of src/types/contract.ts (after existing imports), add:

```typescript
import type { MergedFinding as Finding } from '../schemas/finding';
```

Keep the existing re-export on line 134 (`export type { MergedFinding as Finding } from '../schemas/finding';`) unchanged -- it serves external consumers. The new import brings `Finding` into local scope so the `Contract` interface at line 164 (`findings: Finding[]`) resolves correctly.

This fixes tsc TS2304: Cannot find name 'Finding'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "contract.ts" || echo "0 errors in contract.ts"</automated>
  </verify>
  <done>contract.ts compiles under tsc --noEmit with no TS2304 error for Finding</done>
</task>

<task type="auto">
  <name>Task 2: Fix migration cast in contractStorage.ts</name>
  <files>src/storage/contractStorage.ts</files>
  <action>
In src/storage/contractStorage.ts, in the migrateContracts() function, change line 26 from:

```typescript
return contract as Contract;
```

to:

```typescript
return contract as unknown as Contract;
```

The double-cast through `unknown` is the standard TypeScript pattern for intentional coercion when the source type (`Record<string, unknown>` spread result) doesn't structurally overlap with the target (`Contract`). Runtime behavior is identical -- this only satisfies the strict type checker.

This fixes tsc TS2352.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "contractStorage.ts" || echo "0 errors in contractStorage.ts"</automated>
  </verify>
  <done>contractStorage.ts compiles under tsc --noEmit with no TS2352 error</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` and confirm zero phase-30-introduced errors. The pre-existing TS6133 in CoverageComparisonTab.tsx (unused variable `i`) is not in scope for this plan.

```bash
npx tsc --noEmit
npm run build
npm run lint
```
</verification>

<success_criteria>
- `npx tsc --noEmit` produces zero errors related to contract.ts or contractStorage.ts
- `npm run build` continues to pass
- `npm run lint` continues to pass
- No runtime behavior changes
</success_criteria>

<output>
After completion, create `.planning/phases/30-type-safety-hardening/30-03-SUMMARY.md`
</output>
