---
phase: 26-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/ContractReview.tsx
  - src/pages/Dashboard.tsx
autonomous: true
requirements:
  - PIPE-01

must_haves:
  truths:
    - "Compound Risk findings appear in the by-category tab of ContractReview"
    - "Re-analyze dialog message accurately describes finding preservation behavior"
    - "No disabled placeholder buttons exist in the UI"
  artifacts:
    - path: "src/pages/ContractReview.tsx"
      provides: "CATEGORY_ORDER with Compound Risk, corrected dialog message, Share button removed"
      contains: "Compound Risk"
    - path: "src/pages/Dashboard.tsx"
      provides: "Monthly Report placeholder removed"
  key_links:
    - from: "src/pages/ContractReview.tsx"
      to: "CATEGORY_ORDER array"
      via: "useMemo grouping logic"
      pattern: "Compound Risk"
---

<objective>
Close all v1.4 milestone audit gaps: add missing 'Compound Risk' category to ContractReview.tsx CATEGORY_ORDER, fix re-analyze dialog message, and remove placeholder Share/Monthly Report buttons.

Purpose: Complete the v1.4 milestone by fixing the integration gap that hides synthesis findings and removing misleading UI elements.
Output: Two updated page components with no remaining audit gaps.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-audit-gap-closure/26-CONTEXT.md
@.planning/phases/26-audit-gap-closure/26-RESEARCH.md
@src/pages/ContractReview.tsx
@src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ContractReview.tsx - CATEGORY_ORDER, dialog message, Share button</name>
  <files>src/pages/ContractReview.tsx</files>
  <action>
Three changes to ContractReview.tsx:

1. **Add 'Compound Risk' to CATEGORY_ORDER** (around line 47-57): Add `'Compound Risk'` as the last entry in the CATEGORY_ORDER array, after 'Risk Assessment'. This matches ContractComparison.tsx line 23 per user decision.

2. **Fix re-analyze dialog message** (around line 345): Replace the current message string with exactly: "Re-analyzing will refresh all findings. Resolved status and notes will be preserved where findings match. Select a PDF to continue." — per user locked decision.

3. **Remove Share button** (around lines 273-280): Delete the entire `<button disabled title="Coming soon" ...>` block containing the Share2 icon and "Share" text. Also remove `Share2` from the lucide-react import statement (line ~19). Share2 is ONLY used by the Share button — confirmed by research.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && grep -n "Compound Risk" src/pages/ContractReview.tsx && grep -n "preserved where findings match" src/pages/ContractReview.tsx && ! grep -n "Share2" src/pages/ContractReview.tsx && ! grep -n "Coming soon" src/pages/ContractReview.tsx && npm run build</automated>
  </verify>
  <done>CATEGORY_ORDER includes 'Compound Risk' as last entry. Dialog message reflects preservation behavior. Share button and Share2 import removed. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Monthly Report placeholder from Dashboard.tsx</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
Remove the "Generate Monthly Report" placeholder button (around lines 178-185): Delete the entire `<button disabled title="Coming soon" ...>` block containing "Generate Monthly Report" text.

IMPORTANT: Keep the `FileText` import from lucide-react — it is still used elsewhere in Dashboard.tsx (stat card icon around line 114). Only remove the button JSX.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && ! grep -n "Coming soon" src/pages/Dashboard.tsx && ! grep -n "Monthly Report" src/pages/Dashboard.tsx && grep -n "FileText" src/pages/Dashboard.tsx && npm run build</automated>
  </verify>
  <done>Monthly Report placeholder button removed. FileText import retained (still used by stat card). No "Coming soon" text remains in Dashboard.tsx. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript or lint errors
2. `grep -rn "Coming soon" src/pages/` returns no results
3. `grep -n "Compound Risk" src/pages/ContractReview.tsx` shows the category in CATEGORY_ORDER
4. `grep -n "preserved where findings match" src/pages/ContractReview.tsx` confirms new dialog message
5. `grep -n "Share2" src/pages/ContractReview.tsx` returns no results (import removed)
</verification>

<success_criteria>
- Compound Risk category appears in CATEGORY_ORDER so synthesis findings display in by-category tab
- Re-analyze dialog accurately describes finding preservation behavior
- No disabled placeholder buttons with "Coming soon" tooltips exist anywhere in the UI
- Production build succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-audit-gap-closure/26-01-SUMMARY.md`
</output>
