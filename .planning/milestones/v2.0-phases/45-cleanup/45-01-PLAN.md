---
phase: 45-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/contractStorage.ts
  - src/storage/contractStorage.test.ts
  - src/data/mockContracts.ts
  - src/knowledge/profileLoader.ts
  - src/storage/storageManager.ts
  - src/storage/storageManager.test.ts
  - src/hooks/__tests__/useContractStore.test.ts
  - src/App.test.tsx
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "No contract, finding, date, or company profile data is read from or written to localStorage"
    - "Mock contract data file does not exist and is not imported anywhere"
    - "storageManager handles only the hide-resolved UI preference key"
    - "Build succeeds with zero TypeScript errors after all deletions"
    - "Test suite passes after stale mocks are removed"
  artifacts:
    - path: "src/storage/storageManager.ts"
      provides: "UI-preference-only storage wrapper"
      contains: "clearcontract:hide-resolved"
  key_links:
    - from: "src/hooks/useContractFiltering.ts"
      to: "src/storage/storageManager.ts"
      via: "import { loadRaw, saveRaw }"
      pattern: "loadRaw.*hide-resolved|saveRaw.*hide-resolved"
---

<objective>
Remove all dead localStorage contract storage code now that Supabase handles all persistence. Delete contractStorage.ts, its test, mockContracts.ts, and orphaned profileLoader.ts. Trim storageManager.ts to only the hide-resolved UI preference key. Fix all stale test mocks that reference deleted modules.

Purpose: Complete the v2.0 migration by eliminating dead code that no longer serves any production purpose, reducing maintenance burden and confusion.
Output: Clean codebase with zero localStorage contract references, passing build, passing tests.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-cleanup/45-RESEARCH.md

<interfaces>
<!-- storageManager.ts exports used by surviving code (useContractFiltering) -->
From src/storage/storageManager.ts:
```typescript
export interface StorageRegistry {
  'clearcontract:hide-resolved': string;  // ONLY surviving key after cleanup
}
export type StorageKey = keyof StorageRegistry;
export interface StorageResult<T> { ok: boolean; data: T | null; error: string | null; quotaExceeded: boolean; }
export function load<K extends StorageKey>(key: K): StorageResult<StorageRegistry[K]>;
export function save<K extends StorageKey>(key: K, value: StorageRegistry[K]): StorageResult<null>;
export function loadRaw<K extends StorageKey>(key: K): StorageResult<string>;
export function saveRaw<K extends StorageKey>(key: K, value: string): StorageResult<null>;
export function remove<K extends StorageKey>(key: K): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and trim storageManager to UI preferences only</name>
  <read_first>
    src/storage/storageManager.ts
    src/storage/contractStorage.ts
    src/data/mockContracts.ts
    src/knowledge/profileLoader.ts
  </read_first>
  <files>
    src/storage/contractStorage.ts (DELETE)
    src/storage/contractStorage.test.ts (DELETE)
    src/data/mockContracts.ts (DELETE)
    src/knowledge/profileLoader.ts (DELETE)
    src/storage/storageManager.ts (MODIFY)
  </files>
  <action>
1. Delete these 4 files entirely:
   - `src/storage/contractStorage.ts` (dead -- no production imports)
   - `src/storage/contractStorage.test.ts` (tests for deleted module)
   - `src/data/mockContracts.ts` (only imported by contractStorage.ts)
   - `src/knowledge/profileLoader.ts` (zero imports anywhere in codebase)

2. Modify `src/storage/storageManager.ts`:
   - Remove the two import lines at top:
     ```
     import type { Contract } from '../types/contract';
     import type { CompanyProfile } from '../knowledge/types';
     ```
   - Replace the StorageRegistry interface with ONLY the surviving key:
     ```typescript
     export interface StorageRegistry {
       'clearcontract:hide-resolved': string;
     }
     ```
   - Remove the 4 dead keys: `clearcontract:contracts`, `clearcontract:contracts-seeded`, `clearcontract:schema-version`, `clearcontract:company-profile`
   - Update the JSDoc comment on `loadRaw` to remove references to "seeded, schema-version" -- change to: "Use for keys that store plain strings (hide-resolved)."
   - Update the JSDoc comment on `saveRaw` similarly: "Use for keys that store plain strings (hide-resolved)."
   - Keep ALL five functions unchanged: `load`, `save`, `loadRaw`, `saveRaw`, `remove` (they are generic over StorageRegistry and will auto-narrow)
   - Keep `StorageKey`, `StorageResult` types unchanged

3. Run `npm run build` to confirm zero TypeScript errors after deletions.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/storage/contractStorage.ts` does not exist
    - File `src/storage/contractStorage.test.ts` does not exist
    - File `src/data/mockContracts.ts` does not exist
    - File `src/knowledge/profileLoader.ts` does not exist
    - `src/storage/storageManager.ts` contains `'clearcontract:hide-resolved': string` in StorageRegistry
    - `src/storage/storageManager.ts` does NOT contain `clearcontract:contracts`
    - `src/storage/storageManager.ts` does NOT contain `clearcontract:company-profile`
    - `src/storage/storageManager.ts` does NOT contain `import type { Contract }`
    - `src/storage/storageManager.ts` does NOT contain `import type { CompanyProfile }`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All 4 dead files deleted, storageManager has only hide-resolved key, build passes cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale test mocks referencing deleted modules</name>
  <read_first>
    src/storage/storageManager.test.ts
    src/hooks/__tests__/useContractStore.test.ts
    src/App.test.tsx
  </read_first>
  <files>
    src/storage/storageManager.test.ts (MODIFY)
    src/hooks/__tests__/useContractStore.test.ts (DELETE)
    src/App.test.tsx (MODIFY)
  </files>
  <action>
1. Rewrite `src/storage/storageManager.test.ts` to use ONLY the `clearcontract:hide-resolved` key. The test structure stays the same (describe blocks for load, save, loadRaw, saveRaw, remove) but every test that referenced `clearcontract:contracts` or `clearcontract:contracts-seeded` must switch to `clearcontract:hide-resolved`. Specific changes:

   - `describe('load')`:
     - "returns ok:true with data:null for missing key" -- change `load('clearcontract:contracts')` to `load('clearcontract:hide-resolved')`
     - "returns parsed JSON for valid stored data" -- change `localStorage.setItem('clearcontract:contracts', JSON.stringify([]))` to `localStorage.setItem('clearcontract:hide-resolved', JSON.stringify('true'))` and update assertion to `expect(result.data).toEqual('true')`
     - "returns ok:false with error for invalid JSON" -- change key from `clearcontract:contracts` to `clearcontract:hide-resolved`

   - `describe('save')`:
     - "stores JSON-stringified value" -- change `save('clearcontract:contracts', [])` to `save('clearcontract:hide-resolved', 'true')` and update `localStorage.getItem` expectation to `'"true"'` (JSON-stringified string)
     - "quotaExceeded test" -- change `save('clearcontract:contracts', [])` to `save('clearcontract:hide-resolved', 'true')`

   - `describe('loadRaw')`:
     - Both tests: change `clearcontract:contracts-seeded` to `clearcontract:hide-resolved`

   - `describe('saveRaw')`:
     - Both tests: change `clearcontract:contracts-seeded` to `clearcontract:hide-resolved`

   - `describe('remove')`:
     - Both tests: change `clearcontract:contracts-seeded` to `clearcontract:hide-resolved`

2. Delete `src/hooks/__tests__/useContractStore.test.ts` entirely. This file tests the OLD localStorage-based hook (mocks contractStorage, tests storageWarning/dismissStorageWarning/migrationWarning which no longer exist on the Supabase-based hook). Rewriting it for Supabase is out of scope for this cleanup phase -- it is a known concern tracked in STATE.md.

3. Modify `src/App.test.tsx`:
   - Remove the `vi.mock('./storage/contractStorage', ...)` block (lines 19-23 approximately). This mock targets a module that no longer exists and the current useContractStore does not import contractStorage.
   - Keep everything else in the file unchanged.

4. Run the full test suite to confirm all tests pass.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/storage/storageManager.test.ts` does NOT contain `clearcontract:contracts`
    - `src/storage/storageManager.test.ts` does NOT contain `clearcontract:contracts-seeded`
    - `src/storage/storageManager.test.ts` contains `clearcontract:hide-resolved` in every test
    - File `src/hooks/__tests__/useContractStore.test.ts` does not exist
    - `src/App.test.tsx` does NOT contain `contractStorage`
    - `npx vitest run` exits with code 0
  </acceptance_criteria>
  <done>All test files updated, stale mocks removed, test suite passes with zero failures</done>
</task>

</tasks>

<verification>
After both tasks complete, run these final checks:

1. `npm run build` -- exits 0 (no TypeScript errors)
2. `npx vitest run` -- all tests pass
3. `grep -r "contractStorage\|loadContracts\|saveContracts" src/ --include="*.ts" --include="*.tsx" -l` -- returns zero results
4. `grep -r "mockContracts\|MOCK_CONTRACTS" src/ --include="*.ts" --include="*.tsx" -l` -- returns zero results
5. `grep -r "profileLoader" src/ --include="*.ts" --include="*.tsx" -l` -- returns zero results
6. `test ! -f src/storage/contractStorage.ts && test ! -f src/data/mockContracts.ts && test ! -f src/knowledge/profileLoader.ts && echo "CLEAN"` -- prints CLEAN
</verification>

<success_criteria>
- Zero localStorage contract/profile storage code remains in src/
- mockContracts.ts is deleted and not imported anywhere
- storageManager.ts registry contains only clearcontract:hide-resolved
- Build succeeds, lint passes, all tests pass
- No stale vi.mock() calls referencing deleted modules
</success_criteria>

<output>
After completion, create `.planning/phases/45-cleanup/45-01-SUMMARY.md`
</output>
