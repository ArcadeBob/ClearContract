---
phase: 56-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge/tokenBudget.ts
  - src/knowledge/registry.ts
  - src/knowledge/__tests__/tokenBudget.test.ts
  - src/knowledge/__tests__/registry.test.ts
  - api/passes.ts
  - api/analyze.ts
  - api/merge.ts
  - api/merge.test.ts
  - api/test-fixtures/pass-responses.ts
  - src/schemas/finding.ts
  - src/types/contract.ts
  - src/components/CostSummaryBar.tsx
  - src/components/ScopeMetaBadge/index.tsx
  - src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "MAX_MODULES_PER_PASS equals 6 and validateTokenBudget enforces the new boundary"
    - "The pass formerly called 'scope-of-work' is now called 'scope-extraction' everywhere in runtime code"
    - "AnalysisPass interface carries an optional stage marker (default 2) for Stage 3 routing"
    - "scope-reconciliation is a registered pass with stage: 3 marker, no system prompt body required yet"
    - "Full test suite (npm test) passes green after rename"
  artifacts:
    - path: "src/knowledge/tokenBudget.ts"
      provides: "Raised module cap constant"
      contains: "MAX_MODULES_PER_PASS = 6"
    - path: "api/passes.ts"
      provides: "Renamed scope pass + AnalysisPass.stage field + scope-reconciliation stub registration"
      contains: "name: 'scope-extraction'"
    - path: "src/knowledge/registry.ts"
      provides: "PASS_KNOWLEDGE_MAP entries renamed and split between extraction/reconciliation"
      contains: "'scope-extraction'"
  key_links:
    - from: "api/passes.ts"
      to: "src/knowledge/registry.ts"
      via: "pass name lookup in PASS_KNOWLEDGE_MAP"
      pattern: "PASS_KNOWLEDGE_MAP\\['scope-extraction'\\]"
    - from: "api/merge.ts"
      to: "api/passes.ts"
      via: "isSpecializedPass hard-coded list must contain renamed pass"
      pattern: "'scope-extraction'"
---

<objective>
Resolve ARCH-03: split the scope-of-work pass into two named passes (`scope-extraction` for Stage 2, `scope-reconciliation` for Stage 3), raise `MAX_MODULES_PER_PASS` from 4 to 6, and add a `stage?: 2 | 3` marker to the `AnalysisPass` interface so Stage 3 orchestration (Plan 03) can filter the registry.

Purpose: Unblocks all downstream v3.0 scope-intel work. The rename is mechanical across ~10 files; the constant bump is a single-line change; the stage marker enables wave filtering.
Output: Renamed pass + raised cap + registered `scope-reconciliation` stub, all tests green.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-architecture-foundation/56-CONTEXT.md
@.planning/phases/56-architecture-foundation/56-RESEARCH.md

<interfaces>
<!-- Current state of AnalysisPass interface, from api/passes.ts lines 26-34 -->
```typescript
export interface AnalysisPass {
  name: string;
  systemPrompt: string;
  userPrompt: string;
  isOverview: boolean;
  isLegal?: boolean;
  isScope?: boolean;
  schema?: z.ZodTypeAny;
}
```

<!-- Current tokenBudget.ts, from src/knowledge/tokenBudget.ts -->
```typescript
export const TOKEN_CAP_PER_MODULE = 10000;
export const MAX_MODULES_PER_PASS = 4;
```

<!-- Current PASS_KNOWLEDGE_MAP scope-of-work entry, from src/knowledge/registry.ts line 7-12 -->
```typescript
'scope-of-work': [
  'ca-title24',
  'div08-scope',
  'standards-validation',
  'contract-forms',
],
```

<!-- Current ScopeMeta discriminator, from src/types/contract.ts line 129 -->
```typescript
passType: 'scope-of-work';
```

<!-- All 'scope-of-work' reference sites (from grep) that MUST be updated to 'scope-extraction' -->
1. api/passes.ts:139 — ANALYSIS_PASSES entry
2. api/analyze.ts:64 — PASSES_RECEIVING_PROFILE set
3. api/merge.ts:267 — passType assignment
4. api/merge.ts:347 — passHandlers map key
5. api/merge.ts:470 — isSpecializedPass hard-coded list
6. src/components/CostSummaryBar.tsx:10,30 — PASS_ORDER array + pretty label map
7. api/test-fixtures/pass-responses.ts:75 — fixture key
8. src/types/contract.ts:129 — ScopeMeta passType literal
9. src/knowledge/registry.ts:7 — PASS_KNOWLEDGE_MAP key
10. src/schemas/finding.ts:118 — z.literal('scope-of-work')
11. src/components/ScopeMetaBadge/index.tsx:11 — badge dispatch map
12. src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx:4 — Extract filter
13. api/merge.test.ts:281-297,628 — test assertions and fixture factory labels
14. src/knowledge/__tests__/registry.test.ts:30-31 — test description + getModulesForPass call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise MAX_MODULES_PER_PASS to 6 and update knowledge tests</name>
  <files>src/knowledge/tokenBudget.ts, src/knowledge/__tests__/tokenBudget.test.ts</files>
  <read_first>
    - src/knowledge/tokenBudget.ts (read current constants and validateTokenBudget)
    - src/knowledge/__tests__/tokenBudget.test.ts (read all expected error messages that reference "max is 4")
    - .planning/phases/56-architecture-foundation/56-CONTEXT.md (confirm decision: "raise MAX_MODULES_PER_PASS from 4 → 6")
  </read_first>
  <action>
    In `src/knowledge/tokenBudget.ts` line 4, change `export const MAX_MODULES_PER_PASS = 4;` to `export const MAX_MODULES_PER_PASS = 6;`. Do NOT change `TOKEN_CAP_PER_MODULE` (stays 10000). Do NOT change the `validateTokenBudget` function — its logic is already parameterized on the constant and will propagate automatically.

    In `src/knowledge/__tests__/tokenBudget.test.ts` around line 53-57: the test imports `MAX_MODULES_PER_PASS` directly and constructs `Array.from({ length: MAX_MODULES_PER_PASS + 1 }, ...)` and asserts the error message contains `` `max is ${MAX_MODULES_PER_PASS}` ``. These assertions derive from the real constant — they should continue to pass without edits. Verify by reading the test file; if any assertion hard-codes "max is 4", update it to "max is 6" OR refactor to use the imported constant.

    Do NOT touch `api/analyze.test.ts:49` or `api/regression.test.ts:49` — those mock `MAX_MODULES_PER_PASS: 5` as intentional boundary-fixed values (per 56-RESEARCH.md Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run src/knowledge/__tests__/tokenBudget.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAX_MODULES_PER_PASS = 6" src/knowledge/tokenBudget.ts` returns a match on line 4
    - `grep -n "MAX_MODULES_PER_PASS = 4" src/knowledge/tokenBudget.ts` returns no matches
    - `npx vitest run src/knowledge/__tests__/tokenBudget.test.ts --reporter=dot` exits 0 with all tests passing
    - `grep -rn "max is 4" src/knowledge/` returns no matches
  </acceptance_criteria>
  <done>Constant raised to 6, token budget tests green, no stray "max is 4" strings remain in src/knowledge/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename scope-of-work to scope-extraction across all runtime + test references</name>
  <files>
    api/passes.ts,
    api/analyze.ts,
    api/merge.ts,
    api/merge.test.ts,
    api/test-fixtures/pass-responses.ts,
    src/types/contract.ts,
    src/schemas/finding.ts,
    src/knowledge/registry.ts,
    src/knowledge/__tests__/registry.test.ts,
    src/components/CostSummaryBar.tsx,
    src/components/ScopeMetaBadge/index.tsx,
    src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx
  </files>
  <read_first>
    - api/passes.ts (ANALYSIS_PASSES entry at line 139, AnalysisPass interface at line 26)
    - api/analyze.ts (PASSES_RECEIVING_PROFILE set at line 60-67)
    - api/merge.ts (passType at line 267, passHandlers at line 347, isSpecializedPass at line 470)
    - api/merge.test.ts (lines 281-297 and 628 for scope-of-work references)
    - api/test-fixtures/pass-responses.ts (the 'scope-of-work' key at line 75)
    - src/types/contract.ts (ScopeMeta discriminated union at line 129)
    - src/schemas/finding.ts (z.literal('scope-of-work') at line 118)
    - src/knowledge/registry.ts (PASS_KNOWLEDGE_MAP at lines 4-26)
    - src/knowledge/__tests__/registry.test.ts (lines 30-31)
    - src/components/CostSummaryBar.tsx (PASS_ORDER and PASS_LABELS at lines 10,30)
    - src/components/ScopeMetaBadge/index.tsx (badge dispatch map at line 11)
    - src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx (Extract filter at line 4)
  </read_first>
  <behavior>
    - After rename: `ScopeMeta` union accepts `passType: 'scope-extraction'` and rejects `'scope-of-work'` at compile time
    - `getModulesForPass('scope-extraction')` returns the module array previously returned by `getModulesForPass('scope-of-work')`
    - `passHandlers['scope-extraction']` resolves to a handler using `ScopeOfWorkFindingSchema` + `convertScopeOfWorkFinding` (schema/converter function names stay unchanged — only the pass-name key changes)
    - `merge.test.ts` "converts scope-of-work findings with scopeMeta" test description can be renamed to "converts scope-extraction findings with scopeMeta"; `scopeMeta.passType` assertion expects `'scope-extraction'`
    - `ScopeMetaBadge` component still renders for the renamed passType via its dispatch map
    - `CostSummaryBar` PASS_ORDER and PASS_LABELS map list `'scope-extraction': 'Scope of Work'` (keep the human-facing label "Scope of Work" — only the internal pass-name key changes)
  </behavior>
  <action>
    Perform a global, mechanical rename of the internal pass name from `'scope-of-work'` to `'scope-extraction'`. The human-facing display label "Scope of Work" in `CostSummaryBar.tsx` PASS_LABELS map MUST stay as the string value — only the map key changes.

    Also add a `stage?: 2 | 3` field to the `AnalysisPass` interface in `api/passes.ts` (default stage is 2 when undefined, interpreted by Plan 03 orchestration).

    Concrete changes, file by file:

    1. **api/passes.ts**:
       - Line 26-34, update `AnalysisPass` interface to include `stage?: 2 | 3;` as a new optional field (after `schema?: z.ZodTypeAny;`).
       - Line 139, change `name: 'scope-of-work',` to `name: 'scope-extraction',`. Keep everything else identical (schema, prompts, isScope, isOverview).
       - At end of ANALYSIS_PASSES array, add a new entry BEFORE the closing `];`:
         ```typescript
         {
           name: 'scope-reconciliation',
           isOverview: false,
           isScope: true,
           stage: 3,
           schema: undefined,
           systemPrompt: 'PLACEHOLDER: scope-reconciliation prompt lands in Phase 57/59',
           userPrompt: 'PLACEHOLDER: scope-reconciliation user prompt lands in Phase 57/59',
         },
         ```
         NOTE: This entry is a stub so the registry carries the `stage: 3` marker; it has no knowledge modules mapped yet and will be excluded from Stage 2 by the stage filter introduced in Plan 03. Since the orchestration code in Plan 03 filters Stage 2 as `ANALYSIS_PASSES.filter(p => (p.stage ?? 2) === 2)` and Stage 3 as `ANALYSIS_PASSES.filter(p => p.stage === 3)`, this stub will ONLY appear in Stage 3 and Plan 03's empty-wave guard must NOT trigger for it. **If Plan 03 requires Stage 3 to be empty for empty-wave testing, mark this stub with a comment that it is a registered-but-not-yet-wired pass.**

       Wait — re-evaluate: CONTEXT.md Success Criterion #4 says "Stage 3 can run with zero registered passes (empty wave)". To preserve this testable path, the `scope-reconciliation` stub MUST NOT be registered in this task. REMOVE the stub entry. Instead, add only the `stage?: 2 | 3` field to the interface. Plan 03 will register `scope-reconciliation` as part of wiring Stage 3 orchestration OR leave it for Phase 57.

       Revised action for api/passes.ts: (a) add `stage?: 2 | 3;` to AnalysisPass interface, (b) rename line 139 from `'scope-of-work'` to `'scope-extraction'`. Do NOT add any new ANALYSIS_PASSES entries.

    2. **api/analyze.ts** line 64: change `'scope-of-work',` to `'scope-extraction',` inside the `PASSES_RECEIVING_PROFILE` Set literal.

    3. **api/merge.ts**:
       - Line 267: change `passType: 'scope-of-work',` to `passType: 'scope-extraction',`
       - Line 347: change `'scope-of-work': createHandler(ScopeOfWorkFindingSchema, convertScopeOfWorkFinding),` to `'scope-extraction': createHandler(ScopeOfWorkFindingSchema, convertScopeOfWorkFinding),` (keep schema + converter symbols unchanged)
       - Line 470: in `isSpecializedPass` closure, change `'scope-of-work'` to `'scope-extraction'` inside the hard-coded array `['scope-of-work', 'dates-deadlines', 'verbiage-analysis', 'labor-compliance']`.

    4. **api/merge.test.ts**:
       - Line 281: rename test description `'converts scope-of-work findings with scopeMeta'` → `'converts scope-extraction findings with scopeMeta'`
       - Line 288-289: change `fulfilled('scope-of-work', [finding])` → `fulfilled('scope-extraction', [finding])` and `pass('scope-of-work')` → `pass('scope-extraction')`
       - Line 293: change `expect(f.scopeMeta?.passType).toBe('scope-of-work');` → `expect(f.scopeMeta?.passType).toBe('scope-extraction');`
       - Line 297: change `expect(f.sourcePass).toBe('scope-of-work');` → `expect(f.sourcePass).toBe('scope-extraction');`
       - Line 628: change `{ name: 'scope-of-work', factory: () => createScopeOfWorkFinding() },` → `{ name: 'scope-extraction', factory: () => createScopeOfWorkFinding() },` (keep factory function name unchanged)

    5. **api/test-fixtures/pass-responses.ts** line 75: change key `'scope-of-work': {` → `'scope-extraction': {`. The internal fixture object body stays identical.

    6. **src/types/contract.ts** line 129: change `passType: 'scope-of-work';` → `passType: 'scope-extraction';` inside the ScopeMeta discriminated union.

    7. **src/schemas/finding.ts** line 118: change `passType: z.literal('scope-of-work'),` → `passType: z.literal('scope-extraction'),`.

    8. **src/knowledge/registry.ts**:
       - Lines 7-12: change key `'scope-of-work':` to `'scope-extraction':`. Keep the module id array `['ca-title24', 'div08-scope', 'standards-validation', 'contract-forms']` unchanged (all 4 modules stay with scope-extraction for Phase 56; redistribution across extraction/reconciliation happens in Phase 58 when AAMA + Div 08 new modules arrive).

    9. **src/knowledge/__tests__/registry.test.ts**:
       - Line 30: change test description `'getModulesForPass returns mapped modules for scope-of-work'` → `'getModulesForPass returns mapped modules for scope-extraction'`
       - Line 31: change `getModulesForPass('scope-of-work')` → `getModulesForPass('scope-extraction')`

    10. **src/components/CostSummaryBar.tsx**:
        - Line 10: in PASS_ORDER array, change `'scope-of-work',` → `'scope-extraction',`
        - Line 30: in PASS_LABELS map, change `'scope-of-work': 'Scope of Work',` → `'scope-extraction': 'Scope of Work',` (key renamed, label value "Scope of Work" unchanged — user-facing display must remain identical)

    11. **src/components/ScopeMetaBadge/index.tsx** line 11: in dispatch map, change `'scope-of-work': ScopeOfWorkBadge,` → `'scope-extraction': ScopeOfWorkBadge,` (component export name stays ScopeOfWorkBadge — only the discriminator key changes)

    12. **src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx** line 4: change `type ScopeOfWorkMeta = Extract<ScopeMeta, { passType: 'scope-of-work' }>;` → `type ScopeOfWorkMeta = Extract<ScopeMeta, { passType: 'scope-extraction' }>;`

    After all edits, run `grep -rn "scope-of-work" src/ api/ --include="*.ts" --include="*.tsx"` — it MUST return zero hits. Planning/milestone docs may still reference the old name; that is intentional (historical record).
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "scope-of-work" src/ api/ --include="*.ts" --include="*.tsx"` returns zero matches
    - `grep -rn "scope-extraction" src/ api/ --include="*.ts" --include="*.tsx"` returns at least 12 matches (one per updated file)
    - `grep -n "stage?: 2 | 3" api/passes.ts` returns a match inside the AnalysisPass interface block
    - `grep -n "'Scope of Work'" src/components/CostSummaryBar.tsx` returns a match (user-facing label preserved)
    - `npx vitest run --reporter=dot` exits 0 with ALL tests passing (no regressions)
    - `npx tsc --noEmit` exits 0 (TypeScript compilation clean after discriminator rename)
  </acceptance_criteria>
  <done>All 12 files updated, zero `scope-of-work` runtime references remain, AnalysisPass interface gains `stage?: 2 | 3` field, full test suite and TypeScript compilation green.</done>
</task>

</tasks>

<verification>
Run full suite: `npx vitest run` — must be all-green.
Run lint: `npm run lint` — must be all-green.
Run TypeScript check: `npx tsc --noEmit` — must be all-green.
Run build: `npm run build` — must succeed (Vite will catch discriminator type errors in components if any slipped through).
Grep sweep: `grep -rn "scope-of-work" src/ api/ --include="*.ts" --include="*.tsx"` must return zero matches.
</verification>

<success_criteria>
- `MAX_MODULES_PER_PASS` constant is 6 in `src/knowledge/tokenBudget.ts`
- `AnalysisPass` interface has `stage?: 2 | 3` field
- Pass is named `'scope-extraction'` (not `'scope-of-work'`) across all runtime and test code
- All tests pass, TypeScript compiles, lint clean, build succeeds
- Human-facing label "Scope of Work" still appears in `CostSummaryBar.tsx` PASS_LABELS values
</success_criteria>

<output>
After completion, create `.planning/phases/56-architecture-foundation/56-01-SUMMARY.md`
</output>
