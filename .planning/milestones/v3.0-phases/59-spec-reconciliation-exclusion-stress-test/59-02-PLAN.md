---
phase: 59-spec-reconciliation-exclusion-stress-test
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx
  - src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx
  - src/components/ScopeMetaBadge/index.tsx
  - src/components/ClauseQuote.tsx
  - src/components/FindingCard.tsx
autonomous: true
requirements:
  - SCOPE-03
  - SCOPE-04

must_haves:
  truths:
    - "Spec-reconciliation findings display gap type, spec section, and typical deliverable pills in the finding card"
    - "Exclusion stress-test findings display tension type and spec section pills in the finding card"
    - "Exclusion stress-test findings show both the exclusion quote and the tension quote as distinct ClauseQuote blocks"
    - "Tension quote block uses amber border to visually distinguish inference from contract language"
  artifacts:
    - path: "src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx"
      provides: "Pill row badge for spec-reconciliation findings"
      exports: ["SpecReconciliationBadge"]
    - path: "src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx"
      provides: "Pill row badge for exclusion-stress-test findings"
      exports: ["ExclusionStressTestBadge"]
    - path: "src/components/ScopeMetaBadge/index.tsx"
      provides: "Updated BADGE_MAP with both new pass types"
      contains: "'spec-reconciliation': SpecReconciliationBadge"
    - path: "src/components/ClauseQuote.tsx"
      provides: "Optional borderColor and label props for amber inference quotes"
      contains: "borderColor"
    - path: "src/components/FindingCard.tsx"
      provides: "Dual-quote rendering for exclusion-stress-test findings"
      contains: "exclusion-stress-test"
  key_links:
    - from: "src/components/ScopeMetaBadge/index.tsx"
      to: "src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx"
      via: "BADGE_MAP dispatch"
      pattern: "'spec-reconciliation': SpecReconciliationBadge"
    - from: "src/components/FindingCard.tsx"
      to: "src/components/ClauseQuote.tsx"
      via: "Dual-quote rendering with borderColor prop"
      pattern: "borderColor.*border-amber-300"
---

<objective>
Add the UI components for displaying spec-reconciliation and exclusion-stress-test findings -- ScopeMetaBadge variants, ClauseQuote extension for amber-border inference quotes, and FindingCard dual-quote block.

Purpose: Findings from Plan 01's passes render through the existing FindingCard + ScopeMetaBadge pipeline. This plan adds the two new badge variants and the exclusion-stress-test dual-quote display so users see both the exclusion clause and the inferred tension requirement.

Output: Two new badge components registered in BADGE_MAP, ClauseQuote with optional border color, and FindingCard showing paired quotes for exclusion-stress-test findings.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-spec-reconciliation-exclusion-stress-test/59-UI-SPEC.md
@.planning/phases/59-spec-reconciliation-exclusion-stress-test/59-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ScopeMetaBadge/shared.ts:
```typescript
export const pillBase = 'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium mr-2';
export function formatLabel(value: string): string {
  return value.replace(/-/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase());
}
```

From src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx (reference pattern):
```typescript
import { ScopeMeta } from '../../types/contract';
import { pillBase, formatLabel } from './shared';
type ScopeOfWorkMeta = Extract<ScopeMeta, { passType: 'scope-extraction' }>;
interface ScopeOfWorkBadgeProps { meta: ScopeOfWorkMeta; }
export function ScopeOfWorkBadge({ meta }: ScopeOfWorkBadgeProps) { ... }
```

From src/components/ScopeMetaBadge/index.tsx:
```typescript
import { ScopeMeta } from '../../types/contract';
type PassType = ScopeMeta['passType'];
const BADGE_MAP: Record<PassType, React.FC<{ meta: any }>> = {
  'scope-extraction': ScopeOfWorkBadge,
  'dates-deadlines': DatesDeadlinesBadge,
  'verbiage': VerbiageBadge,
  'labor-compliance': LaborComplianceBadge,
};
```

From src/components/ClauseQuote.tsx:
```typescript
interface ClauseQuoteProps { text: string; reference: string; }
export function ClauseQuote({ text, reference }: ClauseQuoteProps) {
  return (
    <div className="bg-slate-50 border-l-4 border-slate-300 rounded-r-md p-4 my-3">
      <p className="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">
        Contract Language &mdash; {reference}
      </p>
      <p className="text-sm text-slate-700 leading-relaxed whitespace-pre-wrap font-mono">{text}</p>
    </div>
  );
}
```

From src/components/FindingCard.tsx (relevant section):
```typescript
{finding.clauseText && finding.clauseText !== 'N/A - Protective clause absent' && (
  <ClauseQuote text={finding.clauseText} reference={finding.clauseReference || ''} />
)}
...
{finding.scopeMeta && <ScopeMetaBadge meta={finding.scopeMeta} />}
```

From src/types/contract.ts (Plan 01 adds these ScopeMeta variants):
```typescript
| { passType: 'spec-reconciliation'; specSection: string; typicalDeliverable: string; gapType: string; }
| { passType: 'exclusion-stress-test'; exclusionQuote: string; tensionQuote: string; specSection: string; tensionType: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SpecReconciliationBadge and ExclusionStressTestBadge components + BADGE_MAP registration</name>
  <files>
    src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx,
    src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx,
    src/components/ScopeMetaBadge/index.tsx
  </files>
  <read_first>
    src/components/ScopeMetaBadge/ScopeOfWorkBadge.tsx,
    src/components/ScopeMetaBadge/shared.ts,
    src/components/ScopeMetaBadge/index.tsx,
    src/types/contract.ts
  </read_first>
  <action>
**1. Create `src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx`:**

Follow ScopeOfWorkBadge.tsx pattern exactly:
```typescript
import { ScopeMeta } from '../../types/contract';
import { pillBase, formatLabel } from './shared';

type SpecReconciliationMeta = Extract<ScopeMeta, { passType: 'spec-reconciliation' }>;

interface SpecReconciliationBadgeProps {
  meta: SpecReconciliationMeta;
}

export function SpecReconciliationBadge({ meta }: SpecReconciliationBadgeProps) {
  return (
    <div className="flex flex-wrap items-center gap-1 mt-2">
      <span className={`${pillBase} bg-amber-100 text-amber-700`}>
        {formatLabel(meta.gapType)}
      </span>
      <span className={`${pillBase} bg-blue-100 text-blue-700`}>
        {meta.specSection}
      </span>
      {meta.typicalDeliverable &&
        meta.typicalDeliverable !== 'N/A' && (
          <span className={`${pillBase} bg-purple-100 text-purple-700`}>
            {meta.typicalDeliverable.length > 40
              ? meta.typicalDeliverable.slice(0, 40) + '...'
              : meta.typicalDeliverable}
          </span>
        )}
    </div>
  );
}
```

Color rationale per UI-SPEC:
- Gap type: `bg-amber-100 text-amber-700` (inference warning)
- Spec section: `bg-blue-100 text-blue-700` (consistent with specification-reference pill)
- Typical deliverable: `bg-purple-100 text-purple-700` (knowledge-module grounding, consistent with affectedTrade pill)

**2. Create `src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx`:**

```typescript
import { ScopeMeta } from '../../types/contract';
import { pillBase, formatLabel } from './shared';

type ExclusionStressTestMeta = Extract<ScopeMeta, { passType: 'exclusion-stress-test' }>;

interface ExclusionStressTestBadgeProps {
  meta: ExclusionStressTestMeta;
}

export function ExclusionStressTestBadge({ meta }: ExclusionStressTestBadgeProps) {
  return (
    <div className="flex flex-wrap items-center gap-1 mt-2">
      <span className={`${pillBase} bg-amber-100 text-amber-700`}>
        {formatLabel(meta.tensionType)}
      </span>
      <span className={`${pillBase} bg-blue-100 text-blue-700`}>
        {meta.specSection}
      </span>
    </div>
  );
}
```

NOTE: exclusionQuote and tensionQuote are NOT rendered here -- they render as ClauseQuote blocks in FindingCard (Task 2).

**3. Update `src/components/ScopeMetaBadge/index.tsx`:**

Add imports:
```typescript
import { SpecReconciliationBadge } from './SpecReconciliationBadge';
import { ExclusionStressTestBadge } from './ExclusionStressTestBadge';
```

Add to BADGE_MAP:
```typescript
'spec-reconciliation': SpecReconciliationBadge,
'exclusion-stress-test': ExclusionStressTestBadge,
```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx contains `export function SpecReconciliationBadge`
    - src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx contains `bg-amber-100 text-amber-700`
    - src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx contains `bg-blue-100 text-blue-700`
    - src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx contains `bg-purple-100 text-purple-700`
    - src/components/ScopeMetaBadge/SpecReconciliationBadge.tsx contains `meta.typicalDeliverable.length > 40`
    - src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx contains `export function ExclusionStressTestBadge`
    - src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx contains `bg-amber-100 text-amber-700`
    - src/components/ScopeMetaBadge/ExclusionStressTestBadge.tsx contains `formatLabel(meta.tensionType)`
    - src/components/ScopeMetaBadge/index.tsx contains `'spec-reconciliation': SpecReconciliationBadge`
    - src/components/ScopeMetaBadge/index.tsx contains `'exclusion-stress-test': ExclusionStressTestBadge`
    - npx vitest run --reporter=verbose exits 0
  </acceptance_criteria>
  <done>Both badge components render correct pill rows with correct colors per UI-SPEC, and BADGE_MAP dispatches to them for the new passType values.</done>
</task>

<task type="auto">
  <name>Task 2: ClauseQuote borderColor prop + FindingCard dual-quote block for exclusion-stress-test</name>
  <files>
    src/components/ClauseQuote.tsx,
    src/components/FindingCard.tsx
  </files>
  <read_first>
    src/components/ClauseQuote.tsx,
    src/components/FindingCard.tsx,
    src/types/contract.ts
  </read_first>
  <action>
**1. Extend `ClauseQuote` with optional `borderColor` and `label` props:**

Update ClauseQuote.tsx:
```typescript
interface ClauseQuoteProps {
  text: string;
  reference: string;
  borderColor?: string;
  label?: string;
}

export function ClauseQuote({ text, reference, borderColor = 'border-slate-300', label = 'Contract Language' }: ClauseQuoteProps) {
  return (
    <div className={`bg-slate-50 border-l-4 ${borderColor} rounded-r-md p-4 my-3`}>
      <p className="text-xs font-semibold text-slate-500 uppercase tracking-wide mb-2">
        {label} &mdash; {reference}
      </p>
      <p className="text-sm text-slate-700 leading-relaxed whitespace-pre-wrap font-mono">
        {text}
      </p>
    </div>
  );
}
```

This is backward-compatible: existing callers pass no `borderColor`/`label` and get defaults (`border-slate-300` and `"Contract Language"`).

**2. Add dual-quote block in `FindingCard.tsx`:**

After the existing `ClauseQuote` rendering block (the one that checks `finding.clauseText`) and before the "Why This Matters" explanation block, add:

```typescript
{finding.scopeMeta?.passType === 'exclusion-stress-test' &&
  finding.scopeMeta.tensionQuote && (
    <ClauseQuote
      text={finding.scopeMeta.tensionQuote}
      reference={finding.scopeMeta.specSection}
      borderColor="border-amber-300"
      label="Inferred Requirement"
    />
  )}
```

This renders the tension quote with an amber border (distinguishing inference from contract language) per the UI-SPEC dual-quote display contract. The first ClauseQuote (existing) shows the exclusion/contract clause with the default slate border. The second shows the inferred spec requirement with amber.

IMPORTANT: The existing ClauseQuote call for `finding.clauseText` is unchanged -- it uses defaults. The label for the existing block renders "Contract Language" (existing default). For exclusion-stress-test, the first quote shows the exclusion clause text (the `clauseText` field, which the pass populates with the exclusion language), and the second shows the knowledge-module tension requirement.

NOTE: To access `finding.scopeMeta.tensionQuote` in TypeScript, you may need to narrow the type. Use the `passType` check as a type guard:
```typescript
{finding.scopeMeta &&
  finding.scopeMeta.passType === 'exclusion-stress-test' &&
  finding.scopeMeta.tensionQuote && (
    <ClauseQuote ... />
  )}
```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ClauseQuote.tsx contains `borderColor?: string`
    - src/components/ClauseQuote.tsx contains `label?: string`
    - src/components/ClauseQuote.tsx contains `borderColor = 'border-slate-300'`
    - src/components/ClauseQuote.tsx contains `label = 'Contract Language'`
    - src/components/FindingCard.tsx contains `exclusion-stress-test`
    - src/components/FindingCard.tsx contains `border-amber-300`
    - src/components/FindingCard.tsx contains `Inferred Requirement`
    - src/components/FindingCard.tsx contains `tensionQuote`
    - npx vitest run --reporter=verbose exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ClauseQuote supports optional border color and label, FindingCard renders dual-quote display for exclusion-stress-test findings with amber border on the inference quote, and all existing ClauseQuote usage is backward-compatible.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- full test suite green
2. `npx tsc --noEmit` -- no TypeScript compilation errors
3. `npx vite build` -- production build succeeds
4. Grep: `grep "SpecReconciliationBadge\|ExclusionStressTestBadge" src/components/ScopeMetaBadge/index.tsx` shows both entries
5. Grep: `grep "border-amber-300" src/components/FindingCard.tsx` shows dual-quote rendering
</verification>

<success_criteria>
- SpecReconciliationBadge renders gap type (amber), spec section (blue), and truncated typical deliverable (purple) pills
- ExclusionStressTestBadge renders tension type (amber) and spec section (blue) pills
- Both badges are registered in BADGE_MAP and dispatched by ScopeMetaBadge
- ClauseQuote accepts optional borderColor and label props with backward-compatible defaults
- FindingCard renders a second amber-bordered ClauseQuote for exclusion-stress-test findings showing the inferred requirement
- All tests pass, TypeScript compiles, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/59-spec-reconciliation-exclusion-stress-test/59-02-SUMMARY.md`
</output>
