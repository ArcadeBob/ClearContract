---
phase: 61-warranty-safety-osha-clause-passes
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - src/components/ScopeMetaBadge/WarrantyBadge.tsx
  - src/components/ScopeMetaBadge/SafetyOshaBadge.tsx
  - src/components/ScopeMetaBadge/index.tsx
  - src/components/CostSummaryBar.tsx
autonomous: true
requirements: [CLS-01, CLS-02]

must_haves:
  truths:
    - "Warranty findings render WarrantyBadge pills showing aspect (emerald), duration (blue, conditional), affected party (slate, conditional)"
    - "Safety findings render SafetyOshaBadge pills showing aspect (red), regulatory reference (amber, conditional), responsible party (slate, conditional)"
    - "ScopeMetaBadge dispatches to WarrantyBadge for passType='warranty' and SafetyOshaBadge for passType='safety-osha'"
    - "CostSummaryBar shows 'Warranty' and 'Safety & OSHA' labels in the pass breakdown"
  artifacts:
    - path: "src/components/ScopeMetaBadge/WarrantyBadge.tsx"
      provides: "WarrantyBadge component with 3 conditional pills"
      contains: "export function WarrantyBadge"
    - path: "src/components/ScopeMetaBadge/SafetyOshaBadge.tsx"
      provides: "SafetyOshaBadge component with 3 conditional pills"
      contains: "export function SafetyOshaBadge"
    - path: "src/components/ScopeMetaBadge/index.tsx"
      provides: "BADGE_MAP entries for warranty and safety-osha"
      contains: "'warranty': WarrantyBadge"
    - path: "src/components/CostSummaryBar.tsx"
      provides: "PASS_LABELS and PASS_ORDER entries for both passes"
      contains: "'warranty': 'Warranty'"
  key_links:
    - from: "src/components/ScopeMetaBadge/index.tsx"
      to: "src/components/ScopeMetaBadge/WarrantyBadge.tsx"
      via: "import and BADGE_MAP registration"
      pattern: "import.*WarrantyBadge.*from.*WarrantyBadge"
    - from: "src/components/ScopeMetaBadge/index.tsx"
      to: "src/components/ScopeMetaBadge/SafetyOshaBadge.tsx"
      via: "import and BADGE_MAP registration"
      pattern: "import.*SafetyOshaBadge.*from.*SafetyOshaBadge"
---

<objective>
Create WarrantyBadge and SafetyOshaBadge pill components and register them in the ScopeMetaBadge dispatch map and CostSummaryBar labels.

Purpose: Findings from the warranty and safety-osha passes (created in Plan 01) need visible metadata pills in FindingCard and labeled entries in the cost summary breakdown.
Output: Both badge components rendering with correct color assignments per UI-SPEC, BADGE_MAP and PASS_LABELS updated.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-warranty-safety-osha-clause-passes/61-RESEARCH.md
@.planning/phases/61-warranty-safety-osha-clause-passes/61-UI-SPEC.md
@.planning/phases/61-warranty-safety-osha-clause-passes/61-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/ScopeMetaBadge/shared.ts:
```typescript
export const pillBase =
  'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium mr-2';

export function formatLabel(value: string): string {
  return value.replace(/-/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase());
}
```

From src/components/ScopeMetaBadge/BidReconciliationBadge.tsx (template):
```typescript
import { ScopeMeta } from '../../types/contract';
import { pillBase, formatLabel } from './shared';

type BidReconciliationMeta = Extract<ScopeMeta, { passType: 'bid-reconciliation' }>;

export function BidReconciliationBadge({ meta }: { meta: BidReconciliationMeta }) {
  return (
    <div className="flex flex-wrap items-center gap-1 mt-2">
      <span className={`${pillBase} bg-emerald-100 text-emerald-700`}>
        {formatLabel(meta.reconciliationType)}
      </span>
      {meta.directionOfRisk && (
        <span className={`${pillBase} bg-slate-100 text-slate-700`}>
          {meta.directionOfRisk.length > 60
            ? meta.directionOfRisk.slice(0, 60) + '...'
            : meta.directionOfRisk}
        </span>
      )}
    </div>
  );
}
```

From src/components/ScopeMetaBadge/index.tsx:
```typescript
import React from 'react';
import { ScopeMeta } from '../../types/contract';
// 7 existing badge imports...

type PassType = ScopeMeta['passType'];

const BADGE_MAP: Record<PassType, React.FC<{ meta: any }>> = {
  'scope-extraction': ScopeOfWorkBadge,
  'dates-deadlines': DatesDeadlinesBadge,
  'verbiage': VerbiageBadge,
  'labor-compliance': LaborComplianceBadge,
  'spec-reconciliation': SpecReconciliationBadge,
  'exclusion-stress-test': ExclusionStressTestBadge,
  'bid-reconciliation': BidReconciliationBadge,
};
```

From src/components/CostSummaryBar.tsx:
```typescript
const PASS_ORDER = [
  'risk-overview', 'dates-deadlines', 'scope-extraction',
  // ... 16 more entries ...
  'bid-reconciliation', 'synthesis',
];

const PASS_LABELS: Record<string, string> = {
  // ... 20 entries ...
  'bid-reconciliation': 'Bid Reconciliation',
  'synthesis': 'Synthesis',
};
```

From src/schemas/finding.ts (after Plan 01):
ScopeMetaSchema includes:
- passType: 'warranty' with warrantyAspect, warrantyDuration, affectedParty
- passType: 'safety-osha' with safetyAspect, regulatoryReference, responsibleParty
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WarrantyBadge and SafetyOshaBadge components</name>
  <files>src/components/ScopeMetaBadge/WarrantyBadge.tsx, src/components/ScopeMetaBadge/SafetyOshaBadge.tsx</files>
  <read_first>
    - src/components/ScopeMetaBadge/BidReconciliationBadge.tsx (template pattern)
    - src/components/ScopeMetaBadge/shared.ts (pillBase, formatLabel)
    - src/types/contract.ts (ScopeMeta type export)
    - .planning/phases/61-warranty-safety-osha-clause-passes/61-UI-SPEC.md (color assignments)
  </read_first>
  <action>
**Create `src/components/ScopeMetaBadge/WarrantyBadge.tsx`:**

```typescript
import { ScopeMeta } from '../../types/contract';
import { pillBase, formatLabel } from './shared';

type WarrantyMeta = Extract<ScopeMeta, { passType: 'warranty' }>;

export function WarrantyBadge({ meta }: { meta: WarrantyMeta }) {
  return (
    <div className="flex flex-wrap items-center gap-1 mt-2">
      <span className={`${pillBase} bg-emerald-100 text-emerald-700`}>
        {formatLabel(meta.warrantyAspect)}
      </span>
      {meta.warrantyDuration && meta.warrantyDuration !== 'N/A' && (
        <span className={`${pillBase} bg-blue-100 text-blue-700`}>
          {meta.warrantyDuration}
        </span>
      )}
      {meta.affectedParty && meta.affectedParty !== 'unspecified' && (
        <span className={`${pillBase} bg-slate-100 text-slate-700`}>
          {formatLabel(meta.affectedParty)}
        </span>
      )}
    </div>
  );
}
```

Color assignments per UI-SPEC:
- Primary pill (warrantyAspect): `bg-emerald-100 text-emerald-700` (always shown)
- Secondary pill (warrantyDuration): `bg-blue-100 text-blue-700` (hidden when empty or "N/A")
- Tertiary pill (affectedParty): `bg-slate-100 text-slate-700` (hidden when "unspecified")

**Create `src/components/ScopeMetaBadge/SafetyOshaBadge.tsx`:**

```typescript
import { ScopeMeta } from '../../types/contract';
import { pillBase, formatLabel } from './shared';

type SafetyOshaMeta = Extract<ScopeMeta, { passType: 'safety-osha' }>;

export function SafetyOshaBadge({ meta }: { meta: SafetyOshaMeta }) {
  return (
    <div className="flex flex-wrap items-center gap-1 mt-2">
      <span className={`${pillBase} bg-red-100 text-red-700`}>
        {formatLabel(meta.safetyAspect)}
      </span>
      {meta.regulatoryReference && meta.regulatoryReference !== 'N/A' && (
        <span className={`${pillBase} bg-amber-100 text-amber-700`}>
          {meta.regulatoryReference}
        </span>
      )}
      {meta.responsibleParty && (
        <span className={`${pillBase} bg-slate-100 text-slate-700`}>
          {meta.responsibleParty}
        </span>
      )}
    </div>
  );
}
```

Color assignments per UI-SPEC:
- Primary pill (safetyAspect): `bg-red-100 text-red-700` (always shown)
- Secondary pill (regulatoryReference): `bg-amber-100 text-amber-700` (hidden when empty or "N/A")
- Tertiary pill (responsibleParty): `bg-slate-100 text-slate-700` (hidden when empty)

NOTE: regulatoryReference is displayed as raw text (e.g., "T8 Section 3210"), NOT run through formatLabel -- it is a regulatory citation, not a kebab-case enum value.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearContract && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScopeMetaBadge/WarrantyBadge.tsx exists and contains `export function WarrantyBadge`
    - src/components/ScopeMetaBadge/WarrantyBadge.tsx contains `bg-emerald-100 text-emerald-700`
    - src/components/ScopeMetaBadge/WarrantyBadge.tsx contains `bg-blue-100 text-blue-700`
    - src/components/ScopeMetaBadge/WarrantyBadge.tsx contains `bg-slate-100 text-slate-700`
    - src/components/ScopeMetaBadge/WarrantyBadge.tsx contains `meta.warrantyDuration !== 'N/A'`
    - src/components/ScopeMetaBadge/WarrantyBadge.tsx contains `meta.affectedParty !== 'unspecified'`
    - src/components/ScopeMetaBadge/SafetyOshaBadge.tsx exists and contains `export function SafetyOshaBadge`
    - src/components/ScopeMetaBadge/SafetyOshaBadge.tsx contains `bg-red-100 text-red-700`
    - src/components/ScopeMetaBadge/SafetyOshaBadge.tsx contains `bg-amber-100 text-amber-700`
    - src/components/ScopeMetaBadge/SafetyOshaBadge.tsx contains `meta.regulatoryReference !== 'N/A'`
    - Both files import from `'../../types/contract'` and `'./shared'`
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>WarrantyBadge renders 3 conditional pills (emerald/blue/slate), SafetyOshaBadge renders 3 conditional pills (red/amber/slate), both compile cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Register badges in BADGE_MAP and add PASS_LABELS/PASS_ORDER entries</name>
  <files>src/components/ScopeMetaBadge/index.tsx, src/components/CostSummaryBar.tsx</files>
  <read_first>
    - src/components/ScopeMetaBadge/index.tsx (current BADGE_MAP, imports)
    - src/components/CostSummaryBar.tsx (PASS_ORDER array, PASS_LABELS record -- first 55 lines)
  </read_first>
  <action>
**In `src/components/ScopeMetaBadge/index.tsx`:**

Add two imports after the existing badge imports:
```typescript
import { WarrantyBadge } from './WarrantyBadge';
import { SafetyOshaBadge } from './SafetyOshaBadge';
```

Add two entries to the `BADGE_MAP` record (after `'bid-reconciliation': BidReconciliationBadge`):
```typescript
  'warranty': WarrantyBadge,
  'safety-osha': SafetyOshaBadge,
```

The BADGE_MAP should now have 9 entries. The `PassType` type is derived from `ScopeMeta['passType']` which includes the new variants from Plan 01's finding.ts changes -- TypeScript will require these entries to satisfy the `Record<PassType, ...>` type.

**In `src/components/CostSummaryBar.tsx`:**

Add two entries to the `PASS_ORDER` array. Insert `'warranty'` and `'safety-osha'` BEFORE `'synthesis'` (synthesis is always last) and AFTER `'bid-reconciliation'`:
```typescript
  'bid-reconciliation',
  'warranty',
  'safety-osha',
  'synthesis',
```

Add two entries to the `PASS_LABELS` record (before `'synthesis': 'Synthesis'`):
```typescript
  'warranty': 'Warranty',
  'safety-osha': 'Safety & OSHA',
```
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/ClearContract && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScopeMetaBadge/index.tsx contains `import { WarrantyBadge } from './WarrantyBadge'`
    - src/components/ScopeMetaBadge/index.tsx contains `import { SafetyOshaBadge } from './SafetyOshaBadge'`
    - src/components/ScopeMetaBadge/index.tsx contains `'warranty': WarrantyBadge`
    - src/components/ScopeMetaBadge/index.tsx contains `'safety-osha': SafetyOshaBadge`
    - src/components/CostSummaryBar.tsx PASS_ORDER array contains `'warranty'` before `'synthesis'`
    - src/components/CostSummaryBar.tsx PASS_ORDER array contains `'safety-osha'` before `'synthesis'`
    - src/components/CostSummaryBar.tsx PASS_LABELS contains `'warranty': 'Warranty'`
    - src/components/CostSummaryBar.tsx PASS_LABELS contains `'safety-osha': 'Safety & OSHA'`
    - `npx tsc --noEmit` passes with no errors
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>BADGE_MAP has 9 entries dispatching to all badge components, PASS_ORDER and PASS_LABELS include warranty and safety-osha with correct human-readable labels, full build succeeds</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero errors
2. `npm run build` -- succeeds
3. `ls src/components/ScopeMetaBadge/WarrantyBadge.tsx src/components/ScopeMetaBadge/SafetyOshaBadge.tsx` -- both exist
4. `grep -c "'warranty'" src/components/ScopeMetaBadge/index.tsx` returns 1
5. `grep -c "'safety-osha'" src/components/ScopeMetaBadge/index.tsx` returns 1
6. `grep "'warranty'" src/components/CostSummaryBar.tsx` shows both PASS_ORDER and PASS_LABELS entries
7. `grep "'safety-osha'" src/components/CostSummaryBar.tsx` shows both PASS_ORDER and PASS_LABELS entries
</verification>

<success_criteria>
- WarrantyBadge renders emerald/blue/slate pills with correct conditional display logic
- SafetyOshaBadge renders red/amber/slate pills with correct conditional display logic
- ScopeMetaBadge dispatches correctly for both new passType values
- CostSummaryBar displays "Warranty" and "Safety & OSHA" labels in correct order
- Full TypeScript compilation and Vite production build succeed
</success_criteria>

<output>
After completion, create `.planning/phases/61-warranty-safety-osha-clause-passes/61-02-SUMMARY.md`
</output>
