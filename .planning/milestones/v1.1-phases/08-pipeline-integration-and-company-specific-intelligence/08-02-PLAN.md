---
phase: 08-pipeline-integration-and-company-specific-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/SeverityBadge.tsx
  - src/components/BidSignalWidget.tsx
  - src/components/CoverageComparisonTab.tsx
  - src/pages/ContractReview.tsx
  - src/hooks/useContractStore.ts
autonomous: false
requirements: [INTEL-01, INTEL-02, INTEL-03, INTEL-04]

must_haves:
  truths:
    - "SeverityBadge shows downgrade annotation when finding has downgradedFrom"
    - "Bid/no-bid traffic light widget appears next to risk score in review header"
    - "Coverage Comparison tab shows table of contract requirements vs company coverage"
    - "Warning banner appears when company profile has empty fields"
    - "Downgraded findings display their new severity with 'was [Original]' annotation"
  artifacts:
    - path: "src/components/SeverityBadge.tsx"
      provides: "Extended badge with downgradedFrom annotation"
      contains: "downgradedFrom"
    - path: "src/components/BidSignalWidget.tsx"
      provides: "Traffic light bid/no-bid widget"
      exports: ["BidSignalWidget"]
    - path: "src/components/CoverageComparisonTab.tsx"
      provides: "Table comparing contract requirements vs company coverage"
      exports: ["CoverageComparisonTab"]
    - path: "src/pages/ContractReview.tsx"
      provides: "Integrated review page with bid signal, coverage tab, and warning banner"
      contains: "BidSignalWidget"
  key_links:
    - from: "src/pages/ContractReview.tsx"
      to: "src/components/BidSignalWidget.tsx"
      via: "import and render next to risk score"
      pattern: "BidSignalWidget"
    - from: "src/pages/ContractReview.tsx"
      to: "src/components/CoverageComparisonTab.tsx"
      via: "tab navigation alongside by-category/by-severity"
      pattern: "CoverageComparisonTab"
    - from: "src/components/CoverageComparisonTab.tsx"
      to: "src/hooks/useCompanyProfile.ts"
      via: "reads current company profile for comparison display"
      pattern: "useCompanyProfile"
---

<objective>
Build the UI components that display company-specific intelligence: severity downgrade annotations, bid/no-bid widget, coverage comparison tab, and incomplete profile warning.

Purpose: Make the AI's company-specific findings visible and actionable in the review page. Users see insurance gaps, bonding pass/fail, severity adjustments, and a clear bid recommendation at a glance.

Output: Extended SeverityBadge, BidSignalWidget, CoverageComparisonTab components integrated into ContractReview page.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-integration-and-company-specific-intelligence/08-CONTEXT.md
@.planning/phases/08-pipeline-integration-and-company-specific-intelligence/08-RESEARCH.md
@.planning/phases/08-pipeline-integration-and-company-specific-intelligence/08-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan consumes -->

From src/types/contract.ts (extended by Plan 01):
```typescript
export interface Finding {
  // ... existing fields
  downgradedFrom?: Severity; // original severity before AI downgrade
}

export type BidSignalLevel = 'bid' | 'caution' | 'no-bid';
export interface BidFactor { name: string; score: number; weight: number; }
export interface BidSignal { level: BidSignalLevel; label: string; score: number; factors: BidFactor[]; }

export interface Contract {
  // ... existing fields
  bidSignal?: BidSignal;
}
```

From src/components/SeverityBadge.tsx (current, to be extended):
```typescript
interface SeverityBadgeProps {
  severity: Severity;
  className?: string;
}
export function SeverityBadge({ severity, className }: SeverityBadgeProps) { ... }
```

From src/hooks/useCompanyProfile.ts:
```typescript
export function useCompanyProfile(): { profile: CompanyProfile; updateField: <K>(key: K, value: CompanyProfile[K]) => void };
```

From src/knowledge/types.ts:
```typescript
export interface CompanyProfile {
  glPerOccurrence: string; glAggregate: string; umbrellaLimit: string; autoLimit: string;
  wcStatutoryState: string; wcEmployerLiability: string;
  bondingSingleProject: string; bondingAggregate: string;
  // ... other fields
}
```

From src/types/contract.ts (existing LegalMeta for insurance):
```typescript
| { clauseType: 'insurance'; coverageItems: InsuranceCoverageItem[]; endorsements: InsuranceEndorsement[]; certificateHolder: string }

export interface InsuranceCoverageItem {
  coverageType: string;
  requiredLimit: string;
  isAboveStandard: boolean;
}
```

From ContractReview.tsx patterns:
- `viewMode` state: `'by-category' | 'by-severity'` -- add 'coverage' option
- Risk score display at line ~132-135: `<span>Risk Score:</span> <span>{contract.riskScore}/100</span>`
- View mode toggle buttons at line ~144-157
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI components -- SeverityBadge extension, BidSignalWidget, CoverageComparisonTab</name>
  <files>src/components/SeverityBadge.tsx, src/components/BidSignalWidget.tsx, src/components/CoverageComparisonTab.tsx</files>
  <action>
1. **Extend `src/components/SeverityBadge.tsx`**:
   - Add `downgradedFrom?: Severity` to `SeverityBadgeProps`
   - When `downgradedFrom` is provided, render the existing badge for the current (new/lower) severity, followed by a small annotation: `<span className="text-xs text-slate-400 font-medium">was {downgradedFrom}</span>`
   - Wrap both in a `<span className="inline-flex items-center gap-1">` container
   - When `downgradedFrom` is NOT provided, render exactly as before (no wrapper needed, keep current output identical)
   - Use the down arrow per user decision: "was High" text (with down arrow symbol if desired, but text is fine)

2. **Create `src/components/BidSignalWidget.tsx`**:
   - Import `BidSignal` from `../types/contract`
   - Traffic light style per user decision: colored circle + label text
   - Color mapping: `bid` = `bg-emerald-500`, `caution` = `bg-amber-500`, `no-bid` = `bg-red-500`
   - Layout: `<div className="flex items-center gap-2">` with a `<div className="w-4 h-4 rounded-full {color}" />` circle and `<span className="text-sm font-medium text-slate-700">{signal.label}</span>` label
   - Also show the bid score as secondary text: `<span className="text-xs text-slate-400">{signal.score}/100</span>`
   - Props: `{ signal: BidSignal }`
   - Export as named export `BidSignalWidget`
   - Use Framer Motion `motion.div` for a subtle fade-in animation (matches existing pattern)

3. **Create `src/components/CoverageComparisonTab.tsx`**:
   - Import `useCompanyProfile` from `../hooks/useCompanyProfile`
   - Import `Finding`, `InsuranceCoverageItem` from `../types/contract`
   - Props: `{ findings: Finding[] }`
   - Extract insurance requirements from findings: filter findings where `legalMeta` has `clauseType === 'insurance'`, then extract `coverageItems` array from each
   - Extract bonding requirements from findings: filter findings where `sourcePass === 'risk-overview'` and title/description contains 'bond' (case-insensitive)
   - For each insurance coverage item, build a comparison row by matching `coverageType` to the corresponding CompanyProfile field:
     - "General Liability" / "GL" -> `profile.glPerOccurrence`
     - "Aggregate" -> `profile.glAggregate`
     - "Umbrella" / "Excess" -> `profile.umbrellaLimit`
     - "Automobile" / "Auto" -> `profile.autoLimit`
     - "Workers Compensation" / "WC" -> `profile.wcEmployerLiability`
   - For bonding, parse requirement amounts from finding description and compare to `profile.bondingSingleProject`
   - Table columns: Requirement | Contract Requires | Your Coverage | Status
   - Status cell values and colors: "Met" (green), "Exceeds" (green), "GAP: $X" (red), "N/A" (slate, when profile field is empty)
   - Use simple `<table>` with Tailwind classes (per research: don't hand-roll custom grid)
   - Show only what the contract requires -- if contract doesn't mention umbrella, no umbrella row (per user decision)
   - Add a small note at the bottom: "Comparison based on profile at time of analysis. Re-analyze after profile changes to update."
   - If no insurance or bonding findings exist, show a centered message: "No insurance or bonding requirements found in this contract."
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - SeverityBadge renders "was [Original]" annotation when downgradedFrom is set
  - BidSignalWidget renders traffic light circle with label and score
  - CoverageComparisonTab renders comparison table from findings' legalMeta vs company profile
  - All components compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire components into ContractReview page with coverage tab, bid signal, and warning banner</name>
  <files>src/pages/ContractReview.tsx, src/hooks/useContractStore.ts</files>
  <action>
1. **Update `src/hooks/useContractStore.ts`**: When updating a contract from analysis results, map the `bidSignal` field from AnalysisResult onto the Contract object. Find the `updateContract` function or wherever analysis results are merged into the contract state. Ensure `bidSignal` is included in the spread/assignment.

2. **Update ContractReview.tsx**:

   a. **Import new components**: `BidSignalWidget` from `../components/BidSignalWidget`, `CoverageComparisonTab` from `../components/CoverageComparisonTab`, `useCompanyProfile` from `../hooks/useCompanyProfile`

   b. **Add bid signal widget next to risk score**: In the header area (around line 132-135 where risk score is displayed), add the `BidSignalWidget` next to the risk score display. Only render if `contract.bidSignal` exists. Layout: risk score on left, bid signal on right, in a flex row with gap.

   c. **Extend view mode type**: Change `ViewMode` from `'by-category' | 'by-severity'` to `'by-category' | 'by-severity' | 'coverage'`. Add a third toggle button "Coverage" alongside the existing "By Category" and "By Severity" buttons. Style consistently with existing toggle pattern.

   d. **Render CoverageComparisonTab**: When `viewMode === 'coverage'`, render `<CoverageComparisonTab findings={contract.findings} />` instead of the findings list. The category filter should be hidden when coverage tab is active.

   e. **Pass downgradedFrom to SeverityBadge**: Find all places where `<SeverityBadge severity={finding.severity} />` is rendered (in FindingCard or inline) and add `downgradedFrom={finding.downgradedFrom}`. Check both ContractReview.tsx and FindingCard.tsx -- if FindingCard renders SeverityBadge, update FindingCard instead.

   f. **Add incomplete profile warning banner**: At the top of the review page (below header, above findings), check if the company profile has any empty string fields using `useCompanyProfile()`. If any field is `''` (empty string -- not default values, which are valid), show a dismissable warning banner:
      - Style: `bg-amber-50 border border-amber-200 rounded-lg p-3 flex items-center justify-between`
      - Text: "Some company profile fields are empty -- insurance and bonding comparison may be incomplete."
      - Dismiss button: X button that sets `showBanner` state to false
      - Use a `useState` for `showBanner` initialized to `true`. Only show banner if there ARE empty fields AND `showBanner` is true.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - BidSignalWidget renders next to risk score when contract has bidSignal
  - Coverage tab appears as third view mode option and shows comparison table
  - Category filter hides when coverage tab is active
  - SeverityBadge shows downgrade annotations throughout findings display
  - Warning banner appears for empty profile fields, is dismissable
  - Production build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify company-specific intelligence UI</name>
  <action>
  Human verifies the complete company-specific intelligence UI visually and functionally.

  What was built: Bid/no-bid widget, severity downgrade annotations, coverage comparison tab, and profile warning banner integrated into the contract review page.

  How to verify:
    1. Start the dev server: `vercel dev --listen 3008 --yes` (check available port first)
    2. Navigate to Settings and verify company profile is populated with Clean Glass defaults
    3. Upload a contract PDF for analysis
    4. On the review page, verify:
       a. Bid/no-bid traffic light widget appears next to the risk score (green/yellow/red circle with label)
       b. Click the "Coverage" tab -- a table should show contract insurance/bonding requirements vs your company coverage with Met/Gap status
       c. If any findings were severity-downgraded, they should show the lower severity badge with "was [Original]" annotation
       d. If any profile fields are empty, a warning banner should appear at the top (dismissable with X)
    5. Go to Settings, clear one insurance field, go back and re-analyze -- warning banner should appear

  Resume signal: Type "approved" or describe issues
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>User approves the complete company-specific intelligence UI</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (production build)
2. BidSignalWidget visible next to risk score on review page
3. Coverage tab shows comparison table with correct Met/Gap statuses
4. Downgraded findings show "was [Original]" annotation
5. Warning banner appears for empty profile fields and can be dismissed
6. All three view modes (by-category, by-severity, coverage) work correctly
</verification>

<success_criteria>
- Review page shows bid/no-bid traffic light next to risk score
- Coverage Comparison tab displays insurance and bonding requirements vs company profile
- Downgraded findings show lowered severity with "was [Original]" annotation in SeverityBadge
- Empty profile fields trigger a dismissable warning banner
- All existing functionality (category filter, severity filter, finding cards) unchanged
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-pipeline-integration-and-company-specific-intelligence/08-02-SUMMARY.md`
</output>
