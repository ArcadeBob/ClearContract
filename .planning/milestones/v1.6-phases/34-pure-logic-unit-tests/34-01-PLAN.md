---
phase: 34-pure-logic-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/factories.ts
  - api/scoring.test.ts
  - src/utils/errors.test.ts
autonomous: true
requirements:
  - UNIT-01
  - UNIT-04
user_setup: []

must_haves:
  truths:
    - "computeRiskScore returns deterministic scores for known finding distributions"
    - "applySeverityGuard upgrades findings referencing CA void-by-law statutes to Critical"
    - "classifyError correctly maps all 5 error branches (network, timeout, storage, API, unknown)"
    - "formatApiError transforms ClassifiedError into ApiErrorResponse structure"
  artifacts:
    - path: "api/scoring.test.ts"
      provides: "Risk scoring unit tests"
      contains: "computeRiskScore"
    - path: "src/utils/errors.test.ts"
      provides: "Comprehensive error classification tests"
      contains: "classifyError"
    - path: "src/test/factories.ts"
      provides: "Pass-specific factory functions for all 15 specialized passes"
      contains: "createIndemnificationFinding"
  key_links:
    - from: "api/scoring.test.ts"
      to: "api/scoring.ts"
      via: "import computeRiskScore, applySeverityGuard"
      pattern: "import.*computeRiskScore.*from.*scoring"
    - from: "src/utils/errors.test.ts"
      to: "src/utils/errors.ts"
      via: "import classifyError, formatApiError"
      pattern: "import.*classifyError.*from.*errors"
---

<objective>
Create pass-specific test factories and write comprehensive unit tests for risk scoring (UNIT-01) and error classification (UNIT-04).

Purpose: Factories are the foundation for all Phase 34 tests, especially merge tests in Plan 03. Scoring and error modules are small, deterministic, and independent -- perfect for Wave 1 parallel execution.
Output: Extended factories.ts, scoring.test.ts, comprehensive errors.test.ts (replacing trivial Phase 33 version)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-pure-logic-unit-tests/34-CONTEXT.md
@.planning/phases/34-pure-logic-unit-tests/34-RESEARCH.md

@src/test/factories.ts
@api/scoring.ts
@src/utils/errors.ts
@src/schemas/legalAnalysis.ts
@src/schemas/scopeComplianceAnalysis.ts
@src/schemas/finding.ts
@src/schemas/analysis.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From api/scoring.ts:
```typescript
export interface ScoreBreakdown { score: number; categories: Array<{ name: string; points: number }>; }
export function computeRiskScore(findings: Array<{ severity: string; category: string; title: string }>): ScoreBreakdown;
export interface SeverityGuardTarget { severity: string; clauseText?: string; explanation?: string; }
export function applySeverityGuard(finding: SeverityGuardTarget): void;

// Constants (not exported but needed for test math):
// SEVERITY_WEIGHTS: Critical=25, High=15, Medium=8, Low=3, Info=0
// CATEGORY_WEIGHTS: Legal Issues=1.0, Financial Terms=1.0, Insurance Requirements=1.0, Risk Assessment=1.0,
//   Scope of Work=0.75, Contract Compliance=0.75, Labor Compliance=0.75, Important Dates=0.75, Technical Standards=0.75, Compound Risk=0
// Formula: score = Math.min(100, Math.max(0, Math.round(50 * Math.log2(1 + rawScore / 25))))
```

From src/utils/errors.ts:
```typescript
export type ErrorType = 'network' | 'api' | 'validation' | 'storage' | 'timeout' | 'unknown';
export interface ClassifiedError { type: ErrorType; userMessage: string; retryable: boolean; originalError: unknown; }
export interface ApiErrorResponse { error: string; type: ErrorType; retryable: boolean; details?: unknown; }
export function classifyError(err: unknown): ClassifiedError;
export function formatApiError(classified: ClassifiedError, details?: unknown): ApiErrorResponse;
```

From src/schemas/legalAnalysis.ts (15 pass-specific finding schemas):
- IndemnificationFindingSchema: category='Legal Issues', riskType (limited|intermediate|broad), hasInsuranceGap (boolean)
- PaymentContingencyFindingSchema: category='Financial Terms', paymentType (pay-if-paid|pay-when-paid), enforceabilityContext
- LiquidatedDamagesFindingSchema: category='Financial Terms', amountOrRate, capStatus (capped|uncapped), proportionalityAssessment
- RetainageFindingSchema: category='Financial Terms', percentage, releaseCondition, tiedTo (sub-work|project-completion|unspecified)
- InsuranceFindingSchema: category='Insurance Requirements', coverageItems[], endorsements[], certificateHolder
- TerminationFindingSchema: category='Legal Issues', terminationType, noticePeriod, compensation, curePeriod
- FlowDownFindingSchema: category='Legal Issues', flowDownScope, problematicObligations[], primeContractAvailable (boolean)
- NoDamageDelayFindingSchema: category='Legal Issues', waiverScope, exceptions[], enforceabilityContext
- LienRightsFindingSchema: category='Legal Issues', waiverType, lienFilingDeadline, enforceabilityContext
- DisputeResolutionFindingSchema: category='Legal Issues', mechanism, venue, feeShifting, mediationRequired (boolean)
- ChangeOrderFindingSchema: category='Legal Issues', changeType, noticeRequired, pricingMechanism, proceedPending (boolean)

From src/schemas/scopeComplianceAnalysis.ts (4 pass-specific finding schemas):
- ScopeOfWorkFindingSchema: category='Scope of Work', scopeItemType, specificationReference, affectedTrade
- DatesDeadlinesFindingSchema: category='Important Dates', periodType, duration, triggerEvent
- VerbiageFindingSchema: category='Contract Compliance', issueType, affectedParty, suggestedClarification
- LaborComplianceFindingSchema: category='Labor Compliance', requirementType, responsibleParty, contactInfo, deadline, checklistItems[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend factories with pass-specific factory functions</name>
  <files>src/test/factories.ts</files>
  <read_first>
    - src/test/factories.ts (current factory implementations)
    - src/schemas/legalAnalysis.ts (all 11 legal finding schemas with required fields)
    - src/schemas/scopeComplianceAnalysis.ts (all 4 scope finding schemas with required fields)
    - src/schemas/analysis.ts (ActionPriorityEnum, ContractDateSchema)
  </read_first>
  <action>
Add 15 pass-specific factory functions to `src/test/factories.ts`, one for each specialized pass schema. Each factory follows the existing `createFinding` pattern: accepts `Partial<z.infer<typeof Schema>>` overrides, spreads over sensible defaults, and validates through `Schema.parse()`.

Factory functions to create (with their unique required fields as defaults):

**Legal pass factories (11):**
1. `createIndemnificationFinding` -- defaults: category='Legal Issues', riskType='limited', hasInsuranceGap=false
2. `createPaymentContingencyFinding` -- defaults: category='Financial Terms', paymentType='pay-when-paid', enforceabilityContext='Standard in CA'
3. `createLiquidatedDamagesFinding` -- defaults: category='Financial Terms', amountOrRate='$500/day', capStatus='capped', proportionalityAssessment='Reasonable'
4. `createRetainageFinding` -- defaults: category='Financial Terms', percentage='10%', releaseCondition='Final completion', tiedTo='sub-work'
5. `createInsuranceFinding` -- defaults: category='Insurance Requirements', coverageItems=[{coverageType:'GL',requiredLimit:'$1M',isAboveStandard:false}], endorsements=[], certificateHolder='Owner'
6. `createTerminationFinding` -- defaults: category='Legal Issues', terminationType='for cause', noticePeriod='30 days', compensation='Work completed to date', curePeriod='10 days'
7. `createFlowDownFinding` -- defaults: category='Legal Issues', flowDownScope='All terms', problematicObligations=[], primeContractAvailable=false
8. `createNoDamageDelayFinding` -- defaults: category='Legal Issues', waiverScope='All delay damages', exceptions=[], enforceabilityContext='Enforceable in CA'
9. `createLienRightsFinding` -- defaults: category='Legal Issues', waiverType='conditional', lienFilingDeadline='90 days', enforceabilityContext='Standard timeline'
10. `createDisputeResolutionFinding` -- defaults: category='Legal Issues', mechanism='arbitration', venue='County of project', feeShifting='Each party bears own', mediationRequired=true
11. `createChangeOrderFinding` -- defaults: category='Legal Issues', changeType='scope change', noticeRequired='5 days written', pricingMechanism='Time and materials', proceedPending=true

**Scope pass factories (4):**
12. `createScopeOfWorkFinding` -- defaults: category='Scope of Work', scopeItemType='exclusion', specificationReference='Section 08 80 00', affectedTrade='Glazing'
13. `createDatesDeadlinesFinding` -- defaults: category='Important Dates', periodType='completion', duration='180 days', triggerEvent='NTP'
14. `createVerbiageFinding` -- defaults: category='Contract Compliance', issueType='ambiguity', affectedParty='Subcontractor', suggestedClarification='Clarify scope boundaries'
15. `createLaborComplianceFinding` -- defaults: category='Labor Compliance', requirementType='prevailing-wage', responsibleParty='Subcontractor', contactInfo='DIR', deadline='Pre-job', checklistItems=[{item:'Certified payroll',deadline:'Weekly',responsibleParty:'Sub',contactInfo:'DIR',status:'required'}]

Each factory must also include the common base fields: severity='Medium', title='Test {PassName} Finding {counter}', description, recommendation, clauseReference='Section X.X', clauseText='Sample clause text', explanation='Sample explanation', crossReferences=[], negotiationPosition='Request amendment', actionPriority='monitor'.

Import all 15 schemas at the top of the file. Each factory uses its own counter variable (module-level, same pattern as existing `_findingCounter`).

All factories must be exported. Do NOT modify existing `createFinding`, `createContract`, or `createContractDate` functions.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/test/factories.ts contains `export function createIndemnificationFinding`
    - src/test/factories.ts contains `export function createPaymentContingencyFinding`
    - src/test/factories.ts contains `export function createLiquidatedDamagesFinding`
    - src/test/factories.ts contains `export function createRetainageFinding`
    - src/test/factories.ts contains `export function createInsuranceFinding`
    - src/test/factories.ts contains `export function createTerminationFinding`
    - src/test/factories.ts contains `export function createFlowDownFinding`
    - src/test/factories.ts contains `export function createNoDamageDelayFinding`
    - src/test/factories.ts contains `export function createLienRightsFinding`
    - src/test/factories.ts contains `export function createDisputeResolutionFinding`
    - src/test/factories.ts contains `export function createChangeOrderFinding`
    - src/test/factories.ts contains `export function createScopeOfWorkFinding`
    - src/test/factories.ts contains `export function createDatesDeadlinesFinding`
    - src/test/factories.ts contains `export function createVerbiageFinding`
    - src/test/factories.ts contains `export function createLaborComplianceFinding`
    - src/test/factories.ts imports from `../schemas/legalAnalysis`
    - src/test/factories.ts imports from `../schemas/scopeComplianceAnalysis`
    - Existing createFinding, createContract, createContractDate functions still present and unchanged
    - `npx vitest run` exits with 0 (no regressions)
  </acceptance_criteria>
  <done>All 15 pass-specific factory functions exported from factories.ts, each validated through its Zod schema, existing factories unchanged, test suite still green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write comprehensive scoring unit tests</name>
  <files>api/scoring.test.ts</files>
  <read_first>
    - api/scoring.ts (complete source -- computeRiskScore formula, SEVERITY_WEIGHTS, CATEGORY_WEIGHTS, applySeverityGuard, VOID_BY_LAW_PATTERNS)
    - src/test/factories.ts (createFinding factory)
  </read_first>
  <behavior>
    computeRiskScore:
    - Empty findings array returns { score: 0, categories: [] }
    - Single Critical finding in Legal Issues (weight 1.0): rawScore=25, score=Math.round(50*Math.log2(1+25/25))=50
    - Single Critical in Scope of Work (weight 0.75): rawScore=18.75, score=Math.round(50*Math.log2(1+18.75/25))=40
    - Single High in Legal Issues: rawScore=15, score=Math.round(50*Math.log2(1+15/25))=36
    - Single Medium in Legal Issues: rawScore=8, score=Math.round(50*Math.log2(1+8/25))=21
    - Single Low in Legal Issues: rawScore=3, score=Math.round(50*Math.log2(1+3/25))=9
    - Single Info finding: rawScore=0, score=0
    - All-Critical (5 findings in 1.0-weight categories): rawScore=125, score=Math.round(50*Math.log2(1+125/25))=Math.round(50*Math.log2(6))=Math.round(50*2.585)=129->capped at 100
    - Compound Risk category (weight 0) contributes 0 points
    - "Analysis Pass Failed:" title prefix is skipped entirely
    - Unknown category contributes 0 points
    - Multiple findings in same category accumulate points
    - Categories array sorted by points descending, rounded to 1 decimal

    applySeverityGuard:
    - Finding with clauseText mentioning "CC 2782" upgrades to Critical
    - Finding with clauseText mentioning "CC 8814" upgrades to Critical
    - Finding with clauseText mentioning "CC 8122" upgrades to Critical
    - Finding with explanation mentioning "Civil Code Section 2782" upgrades to Critical
    - Already-Critical finding stays Critical (early return)
    - Finding with no statute reference stays at original severity
    - Case-insensitive matching works (cc 2782, CC 2782)
  </behavior>
  <action>
Create `api/scoring.test.ts` with `// @vitest-environment node` directive at the top.

Import `computeRiskScore` and `applySeverityGuard` from `./scoring`.

Structure tests in two describe blocks:

**describe('computeRiskScore'):**
- Test empty findings returns score 0 and empty categories
- Test each severity level individually with a Legal Issues (1.0 weight) finding. Pre-compute expected values:
  - Critical: rawScore=25, score=50
  - High: rawScore=15, score=36
  - Medium: rawScore=8, score=21
  - Low: rawScore=3, score=9
  - Info: rawScore=0, score=0
- Test 0.75-weight category (Scope of Work) with Critical: rawScore=18.75, score=40
- Test Compound Risk category (weight 0) produces score=0
- Test "Analysis Pass Failed:" prefix findings are skipped (score=0)
- Test unknown category produces score=0
- Test multiple findings accumulate: 2 Critical in Legal Issues = rawScore=50, score=Math.round(50*Math.log2(3))=79
- Test score capped at 100: use 5+ Critical findings across 1.0-weight categories to exceed cap
- Test categories array is sorted descending by points
- Test categories points are rounded to 1 decimal

**describe('applySeverityGuard'):**
- Test each of the 6 VOID_BY_LAW_PATTERNS: CC 8814, CC 2782, CC 8122, Civil Code Section 8814, Civil Code Section 2782, Civil Code Section 8122
- Test already-Critical finding stays Critical
- Test no-match finding stays at original severity
- Test explanation field is also scanned (not just clauseText)
- Test case-insensitive matching

Use plain object literals for test inputs (not factories) since computeRiskScore takes `{severity, category, title}` -- simpler than MergedFinding.
  </action>
  <verify>
    <automated>npx vitest run api/scoring.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - api/scoring.test.ts exists and starts with `// @vitest-environment node`
    - api/scoring.test.ts contains `describe('computeRiskScore'`
    - api/scoring.test.ts contains `describe('applySeverityGuard'`
    - api/scoring.test.ts contains `expect(result.score).toBe(0)` (empty findings test)
    - api/scoring.test.ts contains `expect(result.score).toBe(50)` (single Critical Legal Issues)
    - api/scoring.test.ts contains `expect(result.score).toBe(100)` (cap test)
    - api/scoring.test.ts contains `'CC 2782'` (severity guard statute test)
    - `npx vitest run api/scoring.test.ts` exits with 0
  </acceptance_criteria>
  <done>All computeRiskScore boundary cases tested (empty, each severity, each weight tier, accumulation, cap at 100, skip patterns), all applySeverityGuard CA statute patterns tested, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace trivial errors.test.ts with comprehensive coverage</name>
  <files>src/utils/errors.test.ts</files>
  <read_first>
    - src/utils/errors.ts (classifyError 5 branches, formatApiError)
    - src/utils/errors.test.ts (existing trivial test to replace)
  </read_first>
  <behavior>
    classifyError:
    - TypeError('Failed to fetch') -> type='network', retryable=true
    - Error with 'NetworkError' in message -> type='network', retryable=true
    - Error with 'HeadersTimeoutError' in message -> type='timeout', retryable=true
    - Error with 'timeout' in message -> type='timeout', retryable=true
    - Error with 'ETIMEDOUT' in message -> type='timeout', retryable=true
    - DOMException('msg', 'QuotaExceededError') -> type='storage', retryable=false
    - DOMException('msg', 'NS_ERROR_DOM_QUOTA_REACHED') -> type='storage', retryable=false
    - Object with status=429 -> type='api', retryable=true, userMessage mentions 'Too many requests'
    - Object with status=401 -> type='api', retryable=false, userMessage mentions 'Authentication'
    - Object with status=500 -> type='api', retryable=false, userMessage contains 'Server error (500)'
    - Plain string error -> type='unknown', retryable=false
    - Regular Error object -> type='unknown', retryable=false, userMessage=error.message

    formatApiError:
    - Transforms ClassifiedError into ApiErrorResponse: error=userMessage, type=type, retryable=retryable
    - When details provided, includes details field
    - When details undefined, omits details field
  </behavior>
  <action>
Replace `src/utils/errors.test.ts` entirely (the existing file has only 2 trivial tests from Phase 33).

Import `classifyError`, `formatApiError`, and types `ClassifiedError`, `ApiErrorResponse` from `./errors`.

**describe('classifyError'):**

Network errors (2 tests):
- `new TypeError('Failed to fetch')` returns type='network', retryable=true, userMessage contains 'Network error'
- `new Error('NetworkError when...')` returns type='network', retryable=true

Timeout errors (3 tests):
- `new Error('HeadersTimeoutError')` returns type='timeout', retryable=true
- `new Error('Request timeout')` returns type='timeout', retryable=true
- `new Error('ETIMEDOUT')` returns type='timeout', retryable=true

Storage errors (2 tests):
- `new DOMException('quota exceeded', 'QuotaExceededError')` returns type='storage', retryable=false
- `new DOMException('quota', 'NS_ERROR_DOM_QUOTA_REACHED')` returns type='storage', retryable=false

API errors (3 tests):
- `{ status: 429 }` returns type='api', retryable=true, userMessage contains 'Too many requests'
- `{ status: 401 }` returns type='api', retryable=false, userMessage contains 'Authentication'
- `{ status: 500 }` returns type='api', retryable=false, userMessage contains 'Server error (500)'

Unknown errors (2 tests):
- `'some string'` returns type='unknown', retryable=false
- `new Error('Something broke')` returns type='unknown', retryable=false, userMessage='Something broke'

**describe('formatApiError'):**
- Takes a ClassifiedError, returns ApiErrorResponse with error=userMessage, type=type, retryable=retryable
- When details provided: `formatApiError(classified, { field: 'x' })` includes `details: { field: 'x' }`
- When details undefined: result does not have `details` key (use `expect(result).not.toHaveProperty('details')`)
  </action>
  <verify>
    <automated>npx vitest run src/utils/errors.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/errors.test.ts contains `describe('classifyError'`
    - src/utils/errors.test.ts contains `describe('formatApiError'`
    - src/utils/errors.test.ts contains `'HeadersTimeoutError'` (timeout branch test)
    - src/utils/errors.test.ts contains `'QuotaExceededError'` (storage branch test)
    - src/utils/errors.test.ts contains `status: 429` (API rate limit test)
    - src/utils/errors.test.ts contains `status: 401` (API auth test)
    - src/utils/errors.test.ts contains at least 12 `it(` blocks (comprehensive coverage)
    - `npx vitest run src/utils/errors.test.ts` exits with 0
  </acceptance_criteria>
  <done>All 5 classifyError branches tested (network, timeout, storage, API, unknown) with 12+ test cases, formatApiError tested with and without details, all tests pass</done>
</task>

</tasks>

<verification>
npx vitest run api/scoring.test.ts src/utils/errors.test.ts --reporter=verbose
</verification>

<success_criteria>
- 15 pass-specific factory functions exported from src/test/factories.ts
- api/scoring.test.ts covers computeRiskScore boundary cases and applySeverityGuard CA statutes
- src/utils/errors.test.ts covers all 5 classifyError branches and formatApiError
- Full test suite passes: `npx vitest run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/34-pure-logic-unit-tests/34-01-SUMMARY.md`
</output>
