---
phase: 13-upload-error-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/UploadZone.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Dropping a non-PDF file shows 'Only PDF files are accepted' inline in the upload zone"
    - "Dropping an oversized PDF shows 'File exceeds 10MB limit (X MB)' inline in the upload zone"
    - "Error message disappears when user starts a new valid drag or drops a valid file"
  artifacts:
    - path: "src/components/UploadZone.tsx"
      provides: "Inline file rejection error display using react-dropzone onDropRejected"
      contains: "onDropRejected"
  key_links:
    - from: "react-dropzone onDropRejected callback"
      to: "error state displayed below drop zone"
      via: "useState for uploadError"
      pattern: "onDropRejected.*setUploadError"
---

<objective>
Add inline error messages to UploadZone when files are rejected by react-dropzone.

Purpose: Currently, dropping a .docx or oversized PDF silently does nothing. Users need immediate feedback.
Output: UploadZone.tsx displays contextual error messages for file type and size rejections.
</objective>

<execution_context>
@C:/Users/relmq/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/relmq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.2-ROADMAP.md
@src/components/UploadZone.tsx

<interfaces>
<!-- react-dropzone onDropRejected provides FileRejection[] -->
<!-- Each FileRejection has: { file: File, errors: Array<{ code: string, message: string }> } -->
<!-- Error codes: "file-invalid-type", "file-too-large", "file-too-small", "too-many-files" -->

From src/components/UploadZone.tsx (current):
```typescript
interface UploadZoneProps {
  onFileSelect: (file: File) => void;
}
// Uses: useDropzone({ accept: { 'application/pdf': ['.pdf'] }, multiple: false, maxSize: 10 * 1024 * 1024 })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onDropRejected handler and inline error display to UploadZone</name>
  <files>src/components/UploadZone.tsx</files>
  <action>
Modify UploadZone.tsx to handle file rejections inline:

1. Add `useState<string | null>(null)` for `uploadError`.

2. Add `onDropRejected` callback to the useDropzone config. It receives `FileRejection[]`. For each rejection, check `errors[0].code`:
   - `"file-invalid-type"` -> set error to `"Only PDF files are accepted"`
   - `"file-too-large"` -> set error to `"File exceeds 10MB limit (${(file.size / 1024 / 1024).toFixed(1)}MB)"` where `file` is `rejection.file`
   - Default -> set error to `"File could not be accepted"`

3. In the existing `onDrop` callback (for accepted files), clear the error: `setUploadError(null)`.

4. Also clear the error when a new drag starts: add `onDragEnter` callback that calls `setUploadError(null)`.

5. Render the error below the "PDF up to 10MB" helper text. When `uploadError` is non-null, show:
   ```
   <div className="mt-4 flex items-center gap-2 text-red-600 text-sm">
     <AlertCircle className="w-4 h-4 shrink-0" />
     <span>{uploadError}</span>
   </div>
   ```
   Wrap in AnimatePresence + motion.div for fade in/out (initial={{ opacity: 0, y: -4 }}, animate={{ opacity: 1, y: 0 }}, exit={{ opacity: 0 }}).

6. When error is active, change the border color to `border-red-400` (replace `border-slate-300`).

7. Import `AlertCircle` from lucide-react, `useState` from react, `motion` and `AnimatePresence` from framer-motion.
  </action>
  <verify>
    <automated>cd C:/Users/relmq/projects/clearcontract && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Dropping a .docx shows "Only PDF files are accepted" with red AlertCircle icon below the drop zone
    - Dropping a 15MB PDF shows "File exceeds 10MB limit (15.0MB)" inline
    - Error clears when user drags a new file or drops a valid PDF
    - Border turns red when error is displayed
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. Manual: drag a .docx onto upload zone -> red error inline
4. Manual: drag a 15MB PDF onto upload zone -> size error inline
5. Manual: drag valid PDF after error -> error clears, upload proceeds
</verification>

<success_criteria>
- File type and size rejection errors display inline in the upload zone
- Errors auto-clear on valid interaction
- No TypeScript or build errors introduced
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.2-phases/13-upload-error-feedback/13-01-SUMMARY.md`
</output>
