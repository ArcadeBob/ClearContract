/** Maximum contract PDF upload size in bytes (10 MB). */
export const MAX_FILE_SIZE = 10 * 1024 * 1024;

/** Maximum bid PDF upload size in bytes (5 MB). */
export const MAX_BID_FILE_SIZE = 5 * 1024 * 1024;
